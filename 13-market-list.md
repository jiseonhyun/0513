# 고구마마켓 (1) — 상품 목록.

> CHAPTER 13 / 27 · PART 3 · 고구마마켓 · 예상 시간 50분

## 이번 챕터의 목표

고구마마켓의 메인 페이지를 만듭니다.

상품 카드가 그리드로 나열되는 목록 페이지입니다.

지금까지 배운 모든 것을 총동원합니다:

- 클코에게 프롬프트 주기 (04장)
- Supabase MCP로 DB 조작 (08장)
- CLAUDE.md로 프로젝트 설명서 만들기 (12장)

## 전체 흐름 미리 보기

```
1단계: 프로젝트 폴더 만들기
2단계: 클코 실행 + Next.js 프로젝트 생성
3단계: CLAUDE.md 생성
4단계: Supabase에 상품 테이블 만들기 (MCP)
5단계: 테스트 데이터 넣기
6단계: 메인 페이지에 상품 목록 표시
7단계: 디자인 다듬기
```

## 1단계: 프로젝트 폴더 만들기

PowerShell을 열고 아래 명령어를 입력합니다:

```
cd C:\claude-projects
mkdir goguma-market
cd goguma-market
```

반드시 `C:\claude-projects` 폴더 안에서 만드세요. (2장에서 만든 작업 폴더)

폴더 이름에 한글이나 공백은 피하세요.

`goguma-market`처럼 영문 소문자 + 하이픈이 가장 안전합니다.

## 2단계: 클코 실행 + 프로젝트 생성

해당 폴더에서 클코를 실행합니다:

```
claude
```

클코가 실행되면 아래 프롬프트를 입력하세요:

```
고구마마켓이라는 중고거래 웹앱을 만들 거야.
Next.js + Supabase + Tailwind CSS로 프로젝트 초기 설정해줘.
TypeScript 사용하고.
```

클코가 `npx create-next-app`을 실행합니다. 설정 질문이 나오면 클코가 알아서 선택합니다.

### 완료 확인

클코가 끝나면 아래를 물어보세요:

```
프로젝트 구조 보여줘
```

`package.json`, `src/app/page.tsx`, `tailwind.config.ts` 등이 보이면 성공입니다.

## 3단계: CLAUDE.md 생성

바로 이어서 CLAUDE.md를 만듭니다:

```
이 프로젝트에 CLAUDE.md 만들어줘.

내용:
```

이제부터 새 대화를 열어도 클코가 프로젝트를 바로 이해합니다.

## 4단계: Supabase에 상품 테이블 만들기

여기서 Supabase MCP가 활약합니다.

클코에게 아래 프롬프트를 입력하세요:

```
Supabase MCP로 상품 테이블 만들어줘.

테이블명: products
컬럼:

id (UUID, 자동 생성, Primary Key)
title (상품명, 텍스트, 필수)
description (상품 설명, 긴 텍스트)
price (가격, 정수, 필수)
image_url (이미지 URL, 텍스트)
seller_name (판매자 이름, 텍스트, 필수)
status (판매상태: 판매중/예약중/판매완료, 기본값 '판매중')
created_at (등록일, 자동 생성)
```

"RLS 비활성화"는 지금은 로그인 기능이 없으니 누구나 데이터를 읽을 수 있게 하는 것입니다.

챕터 15에서 소셜 로그인을 추가할 때 다시 설정합니다.

### 확인

```
products 테이블 잘 만들어졌는지 확인해줘. 컬럼 목록 보여줘.
```

클코가 MCP를 통해 테이블 정보를 보여주면 성공입니다.

## 5단계: 테스트 데이터 넣기

빈 테이블만으로는 화면을 확인할 수 없습니다.

테스트용 상품 데이터를 넣어달라고 합니다:

```
products 테이블에 테스트 데이터 6개 넣어줘.

다양하게:
```

### 확인

```
products 데이터 전부 보여줘
```

6개의 상품이 보이면 성공입니다!

## 6단계: 메인 페이지에 상품 목록 표시

이제 진짜 페이지를 만듭니다:

```
메인 페이지(src/app/page.tsx)에 상품 목록을 보여줘.

요구사항:
```

클코가 여러 파일을 만들거나 수정할 겁니다. 시간이 좀 걸릴 수 있습니다.

### Supabase 연결 정보

클코가 `.env.local` 파일에 Supabase URL과 키를 넣어야 합니다.

이 정보를 물어보면 Supabase 대시보드에서 확인해서 알려주세요:

- Settings → API 에서:
- `NEXT_PUBLIC_SUPABASE_URL`: Project URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`: anon public 키

anon key는 공개해도 되는 키입니다. service_role key와 다릅니다.

## 7단계: 확인하기

```
npm run dev 실행해줘
```

브라우저에서 http://localhost:3000 을 엽니다.

### 확인 체크리스트

- 상품 카드가 화면에 보이는가?
- 상품명과 가격이 제대로 표시되는가?
- 가격이 "₩10,000" 형태인가? (그냥 10000이 아니라)
- 판매 상태 뱃지가 보이는가?
- 브라우저 크기를 줄이면 1열로 바뀌는가? (모바일 반응형)
- "고구마마켓" 헤더가 상단에 있는가?

## 8단계: 디자인 다듬기

기본 기능이 동작하면 디자인을 다듬어봅니다.

아래 프롬프트들을 취향에 맞게 사용하세요:

### 카드 디자인

```
상품 카드에 살짝 그림자(shadow) 넣고,
마우스 올리면 위로 살짝 올라오는 호버 효과 줘
```

### 헤더 디자인

```
헤더를 좀 더 예쁘게 만들어줘.
주황색 배경에 흰색 "고구마마켓" 텍스트.
오른쪽에 "상품 등록" 버튼도 넣어줘 (아직 동작 안 해도 됨)
```

### 빈 상태 처리

```
상품이 없을 때 "등록된 상품이 없습니다" 메시지 보여줘
```

### 전체 분위기 변경

```
전체적인 디자인을 당근마켓 느낌으로 바꿔줘.
따뜻하고 친근한 분위기로.
```

디자인은 정답이 없습니다. 여러분이 원하는 대로 자유롭게 수정하세요!

마음에 안 들면 "원래대로 되돌려줘"라고 하면 됩니다.

## 잘 안 될 때

| 증상 | 원인 | 해결 |
|------|------|------|
| 화면에 아무것도 안 나옴 | Supabase 연결 안 됨 | .env.local에 URL/키가 있는지 확인 |
| 에러: relation "products" does not exist | 테이블이 안 만들어짐 | "products 테이블 다시 만들어줘" |
| 카드는 보이는데 데이터가 비어있음 | RLS 정책 문제 | "RLS 비활성화해줘" |
| 이미지가 안 보임 | image_url이 비어있음 | 정상. 플레이스홀더가 보여야 함 |
| 서버 시작이 안 됨 | 패키지 설치 안 됨 | "npm install 하고 다시 실행해줘" |

## 지금까지 만든 것 GitHub에 올리기

이번 작업이 끝나면 바로 GitHub에 올려두세요:

```
지금까지 작업한 거 GitHub에 올려줘.
저장소 이름은 goguma-market으로.
```

## 핵심 정리

1. 프로젝트 폴더 생성 → 클코 실행 → Next.js 프로젝트 초기화
2. CLAUDE.md로 프로젝트 설명서 세팅
3. Supabase MCP로 DB 테이블 생성 + 테스트 데이터 삽입
4. 메인 페이지에서 Supabase 데이터를 읽어 카드 그리드로 표시
5. 디자인은 프롬프트로 자유롭게 조정 가능
6. 작업 완료 후 GitHub에 업로드

다음 챕터에서는 상품 등록, 상세, 수정, 삭제 기능을 추가합니다.
