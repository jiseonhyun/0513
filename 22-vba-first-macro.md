# 첫 번째 매크로 만들기.

> CHAPTER 22 / 27 · PART 4 · VBA 업무 자동화 · 예상 시간 50분

## 이번 챕터의 목표

업무에 바로 쓸 수 있는 3가지 매크로를 만듭니다.

버튼을 눌러서 실행하는 것까지 해봅니다.

클코에게 요청 → 코드 복사 → VBA에 붙여넣기 → 실행하는 흐름을 손에 익힙니다.

## 전체 워크플로우 복습

매크로를 만드는 과정을 다시 한번 정리합니다:

```
Step 1. 클코에게 프롬프트로 VBA 코드 요청
Step 2. 클코가 준 코드를 복사 (Ctrl+C)
Step 3. 엑셀에서 Alt+F11 → VBA 에디터 열기
Step 4. 삽입 → 모듈 (이미 있으면 기존 모듈 클릭)
Step 5. 코드 창에 붙여넣기 (Ctrl+V)
Step 6. F5로 실행하거나 버튼에 연결
```

이 흐름을 이번 챕터에서 3번 반복합니다.

3번만 해보면 자연스러워집니다.

## 실습 1: 날짜/시간 자동 입력

### 이런 상황에 씁니다

- 일지를 쓸 때마다 오늘 날짜를 직접 입력하고 있다
- 작업 로그에 시간을 기록해야 한다
- Ctrl+; (날짜 입력 단축키)보다 더 편한 방법을 원한다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
현재 선택된 셀에 오늘 날짜와 시간을 입력하는 매크로.
형식은 "2026-03-09 14:30" 이런 식으로.

추가 기능:

이미 내용이 있는 셀이면 덮어쓸지 물어보기
입력 후 커서를 아래 셀로 자동 이동
Sub 이름은 DateStamp로 해줘
```

### 실행 후 확인

- 선택한 셀에 "2026-03-09 14:30" 형식으로 날짜가 입력되었다
- 이미 내용이 있는 셀에서 실행하면 확인 메시지가 나온다
- 입력 후 커서가 아래 셀로 이동했다

### 응용: 형식을 바꾸고 싶으면

클코에게 형식만 바꿔달라고 하면 됩니다:

```
위 코드에서 날짜 형식을 "3월 9일 (월) 오후 2:30"으로 바꿔줘.
```

```
날짜만 입력하는 버전, 시간만 입력하는 버전 2개로 나눠줘.
```

## 실습 2: 선택 영역 서식 정리 (테이블 만들기)

### 이런 상황에 씁니다

- 데이터를 표로 보기 좋게 정리해야 하는데 매번 서식 잡기가 귀찮다
- 보고서마다 같은 서식을 반복 적용하고 있다
- 헤더에 색 넣고, 테두리 치고, 열 너비 맞추는 게 일이다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
선택한 영역을 아래처럼 서식 정리하는 매크로:

첫 번째 행 (헤더):

글꼴: 굵게
배경색: 연한 파란색 (#4472C4)
글자색: 흰색
가운데 정렬

전체 영역:

모든 셀에 얇은 테두리 적용
가운데 정렬
열 너비 자동 맞춤

데이터 행 (2행부터):

짝수 행에 연한 회색 배경 (줄무늬 효과)
```

### 사용 방법

1. 엑셀에서 표로 만들 영역을 마우스로 드래그해서 선택
2. `Alt + F8` → FormatTable 선택 → 실행

또는 뒤에서 배울 버튼을 만들어서 클릭 한 번으로 실행할 수도 있습니다.

### 실행 후 확인

- 첫 번째 행이 파란 배경 + 흰 글씨 + 굵은 글꼴이다
- 전체 영역에 테두리가 적용되었다
- 열 너비가 자동으로 맞춰졌다
- 짝수 행에 줄무늬 효과가 있다

### 응용: 다른 스타일의 테이블

```
위 서식 코드를 수정해서 3가지 스타일 버전을 만들어줘:
1. FormatTable_Blue — 파란 테마 (현재)
2. FormatTable_Green — 초록 테마
3. FormatTable_Simple — 테두리만 (색 없이)
```

## 실습 3: 금액 서식 적용 (원화 표시)

### 이런 상황에 씁니다

- 숫자만 있는 데이터에 ₩와 쉼표를 넣어야 한다
- 50000 → ₩50,000 같은 변환을 매번 수동으로 한다
- 금액이 들어있는 열에 일괄 적용하고 싶다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
선택한 영역의 숫자에 원화 서식을 적용하는 매크로.

표시 형식: ₩50,000 (천 단위 쉼표 + 원화 기호)
음수는 빨간색으로 표시: -₩50,000
소수점은 표시하지 않음
숫자가 아닌 셀은 건너뛰기
오른쪽 정렬
```

### 실행 후 확인

- 50000 → ₩50,000 으로 표시된다
- 음수는 빨간색으로 표시된다
- 텍스트가 들어있는 셀은 변경되지 않았다
- 오른쪽 정렬이 적용되었다

### 응용: 다른 통화

```
달러 버전도 만들어줘. FormatDollar라는 이름으로.
$50,000.00 형식 (소수점 2자리).
```

## 버튼 만들기 — 클릭 한 번으로 실행

코드를 매번 `Alt + F8`에서 찾아 실행하기 귀찮습니다.

엑셀 시트에 버튼을 만들면 클릭 한 번으로 매크로를 실행할 수 있습니다.

### 방법 1: 엑셀 메뉴에서 직접 만들기

1. 개발 도구 탭 클릭
2. 삽입 → 버튼 (양식 컨트롤) 클릭 (첫 번째 아이콘)
3. 시트에서 마우스를 드래그해서 버튼 크기를 그립니다
4. "매크로 지정" 창이 뜨면 연결할 매크로 선택 (예: FormatTable)
5. 확인 클릭
6. 버튼 위에서 우클릭 → 텍스트 편집 → 원하는 이름 입력 (예: "표 서식 적용")

### 방법 2: 클코에게 버튼 코드까지 요청

```
위에서 만든 3개 매크로(DateStamp, FormatTable, FormatCurrency)를
각각 버튼으로 실행할 수 있게 해줘.
```

### 버튼 관련 팁

- 버튼 위치 변경: 버튼 위에서 우클릭 → 드래그로 이동
- 버튼 삭제: 버튼 위에서 우클릭 → Delete
- 버튼이 클릭이 안 될 때: 디자인 모드가 켜져있는지 확인 (개발 도구 → 디자인 모드 끄기)

## 여러 매크로 관리하기

매크로가 늘어나면 정리가 필요합니다.

### 하나의 모듈에 여러 매크로 넣기

VBA 에디터에서 하나의 모듈 안에 여러 개의 `Sub`를 넣을 수 있습니다:

```vba
Sub DateStamp()
    ' 날짜 입력 코드
End Sub

Sub FormatTable()
    ' 서식 정리 코드
End Sub
```

### 클코에게 한번에 요청하기

```
엑셀 VBA 코드 만들어줘.
아래 3개 매크로를 하나의 모듈에 만들어줘:

DateStamp — 선택 셀에 오늘 날짜+시간 입력 (2026-03-09 14:30)
FormatTable — 선택 영역을 테이블 서식으로 정리 (헤더 파란색, 테두리, 줄무늬)
FormatCurrency — 선택 영역 숫자에 ₩50,000 형식 적용
```

### 모듈 이름 바꾸기

기본 모듈 이름은 "Module1"입니다. 알아보기 쉽게 바꿀 수 있습니다:

1. VBA 에디터 왼쪽의 프로젝트 탐색기에서 모듈 클릭
2. 아래 속성 창에서 (Name)을 "도구모음" 등으로 변경

모듈이 여러 개면 용도별로 나누면 편합니다:

- `서식도구` — 서식 관련 매크로
- `데이터처리` — 데이터 정리 매크로
- `보고서` — 보고서 생성 매크로

## 매크로 실행 단축키 설정

자주 쓰는 매크로에 단축키를 지정할 수 있습니다:

1. `Alt + F8` → 매크로 목록 열기
2. 원하는 매크로 선택
3. 옵션 클릭
4. 바로 가기 키에 원하는 키 입력 (예: Ctrl + Shift + D)
5. 확인 클릭

이제 `Ctrl + Shift + D`만 누르면 매크로가 실행됩니다.

주의: Ctrl + C, Ctrl + V 같은 기존 단축키와 겹치지 않도록 하세요.

`Ctrl + Shift + 알파벳` 조합을 추천합니다.

## 문제 해결 가이드

### "이 통합 문서에 매크로가 포함되어 있지 않습니다"

모듈에 코드가 없는 경우입니다.

`Alt + F11` → 삽입 → 모듈 → 코드 붙여넣기를 다시 해보세요.

### 매크로가 원하는 대로 작동하지 않을 때

클코에게 상황을 구체적으로 설명하세요:

```
VBA 코드를 실행했는데 기대한 결과가 아니야.

기대한 결과: 첫 행이 파란 배경이어야 함
실제 결과: 전체 행이 파란 배경이 됨
```

### 매크로 삭제

VBA 에디터에서 해당 코드를 선택 → Delete 키.

모듈 전체를 삭제하려면: 모듈 우클릭 → "Module1 제거"

## 이번 챕터 핵심 정리

- DateStamp 매크로를 만들어서 날짜/시간 자동 입력 성공
- FormatTable 매크로를 만들어서 표 서식 적용 성공
- FormatCurrency 매크로를 만들어서 원화 서식 적용 성공
- 버튼을 만들어서 클릭으로 매크로 실행 가능
- 여러 매크로를 하나의 모듈에 관리하는 방법 이해
- `.xlsm`으로 저장해서 매크로 보존

다음 챕터에서는 데이터 정리를 자동화하는 매크로를 만듭니다.

매일 반복하는 중복 제거, 빈 행 삭제, 데이터 분류 등을 버튼 하나로 끝내봅시다.
