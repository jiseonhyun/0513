# 데이터 정리 자동화.

> CHAPTER 23 / 27 · PART 4 · VBA 업무 자동화 · 예상 시간 50분

## 이번 챕터의 목표

엑셀에서 가장 많이 하는 작업인 데이터 정리를 자동화합니다.

중복 제거, 빈 행 삭제, 부서별 분류, 조건 추출, 형식 통일까지

5가지 핵심 매크로를 만듭니다.

## 시작하기 전에

이번 챕터의 매크로들은 실제 데이터에 적용합니다.

실습 전에 꼭 지키세요:

1. 원본 파일을 복사해두세요 — 매크로를 잘못 실행하면 되돌리기 어려울 수 있습니다
2. Ctrl + Z (실행 취소)가 안 됩니다! — 이것이 가장 중요합니다.

   일반적인 엑셀 작업은 Ctrl+Z로 되돌릴 수 있지만,
   VBA 매크로로 변경한 내용은 Ctrl+Z가 작동하지 않습니다.
   매크로를 실행하면 되돌리기가 불가능하니, 반드시 원본을 백업하세요.

3. 연습용 데이터로 먼저 테스트하세요 — 실제 업무 데이터에 적용하기 전에

백업 방법: 파일 우클릭 → 복사 → 같은 폴더에 붙여넣기 → "데이터_백업.xlsx"로 이름 변경

연습용 데이터가 없다면 클코에게 만들어달라고 하세요:

```
엑셀 VBA 코드 만들어줘.
"연습데이터"라는 시트를 만들고 아래 구조로 샘플 데이터 50행을 넣어줘:
- A열: 이름 (일부 중복)
- B열: 부서 (영업팀, 개발팀, 인사팀, 마케팅팀 중 랜덤)
- C열: 금액 (10만~500만 사이 랜덤)
- D열: 날짜 (2026년 1월~3월 사이 랜덤)
- E열: 전화번호 (다양한 형식으로: 010-1234-5678, 01012345678, 010.1234.5678 등)
- 중간에 빈 행 5개 랜덤으로 넣어줘
- 앞뒤에 불필요한 공백이 있는 데이터도 섞어줘
```

## 실습 1: 중복 데이터 찾기 & 제거

### 이런 상황에 씁니다

- 여러 곳에서 데이터를 합쳤더니 중복이 있다
- 같은 이름, 같은 거래가 여러 번 입력되어 있다
- 중복을 찾아서 확인한 후 제거하고 싶다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
A열에서 중복된 값을 찾아서 처리하는 매크로.

동작 순서:

먼저 중복된 행을 노란색 배경으로 표시
"중복 X개를 발견했습니다. 제거할까요?" 메시지 표시
"예"를 선택하면 중복 행 삭제 (첫 번째 것은 남기고 나머지 삭제)
"아니오"를 선택하면 노란색 표시만 남겨둠
최종 결과를 메시지로 표시: "X개 중복 발견, Y개 제거됨"
```

### 실행 후 확인

- 중복된 행이 노란색으로 표시된다
- 중복 개수가 메시지로 표시된다
- "예" 선택 시 중복이 제거된다 (첫 번째 것은 남음)
- "아니오" 선택 시 노란색 표시만 남아있다

### 응용: 여러 열 기준 중복 확인

```
A열과 B열을 조합해서 중복을 확인하도록 수정해줘.
즉, 이름(A열)과 부서(B열)가 모두 같은 경우만 중복으로 판단.
```

### 응용: 특정 열 기준 변경

```
중복 확인할 열을 매크로 실행 시 선택할 수 있게 해줘.
InputBox로 "중복 확인할 열을 입력하세요 (예: A)" 이런 식으로.
```

## 실습 2: 빈 행 정리 & 공백 제거

### 이런 상황에 씁니다

- 데이터 사이에 빈 행이 군데군데 있다
- 셀 앞뒤에 보이지 않는 공백이 있어서 VLOOKUP이 안 된다
- 줄바꿈 문자가 섞여있어서 데이터가 지저분하다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
현재 시트의 데이터를 깨끗하게 정리하는 매크로.

처리할 작업:

완전히 비어있는 행을 모두 삭제
모든 셀의 앞뒤 공백 제거 (Trim)
셀 안의 연속 공백을 하나의 공백으로 변환 ("김  철수" → "김 철수")
줄바꿈 문자 제거
보이지 않는 특수 문자 제거 (Clean)

처리 결과를 메시지로 표시:

삭제된 빈 행: X개
공백 정리된 셀: Y개
```

### 실행 후 확인

- 빈 행이 모두 삭제되었다
- 셀 앞뒤의 공백이 제거되었다
- 처리 결과가 메시지로 표시된다
- 헤더 행은 변경되지 않았다

### 왜 이게 중요한가

보이지 않는 공백은 엑셀에서 가장 흔한 문제 중 하나입니다.

예를 들어 "영업팀"과 "영업팀 " (뒤에 공백 있음)은 엑셀이 다른 값으로 인식합니다.

그래서 VLOOKUP이 안 되거나, 필터가 제대로 안 되거나, 합계가 맞지 않는 경우가 생깁니다.

이 매크로 하나로 그런 문제를 한번에 해결할 수 있습니다.

## 실습 3: 부서별 데이터 분류 (시트 분리)

### 이런 상황에 씁니다

- 전체 데이터가 하나의 시트에 있는데 부서별로 나눠서 보내야 한다
- 분류별/지역별/담당자별로 각각 다른 시트에 정리하고 싶다
- 수동으로 필터 → 복사 → 새 시트에 붙여넣기를 반복하고 있다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
B열의 "부서명"을 기준으로 데이터를 부서별 시트로 분류하는 매크로.

동작:

B열에서 고유한 부서명 목록을 추출
각 부서명으로 새 시트 생성 (이미 있으면 기존 시트의 데이터 지우고 재사용)
해당 부서의 데이터를 새 시트로 복사
각 시트에도 헤더(1행)를 포함
원본 시트("데이터")는 그대로 유지
완료 후 메시지: "X개 부서로 분류 완료 (영업팀: 15건, 개발팀: 12건, ...)"
```

### 실행 후 확인

- 부서별로 새 시트가 생성되었다
- 각 시트에 해당 부서 데이터만 들어있다
- 각 시트에 헤더가 포함되어 있다
- 원본 "데이터" 시트는 변경되지 않았다
- 완료 메시지에 각 부서별 건수가 표시된다

### 응용: 다른 기준으로 분류

프롬프트에서 "B열의 부서명"만 바꾸면 됩니다:

```
위 코드를 수정해서 D열의 "월"을 기준으로 분류해줘.
"1월", "2월", "3월" 이런 식으로 시트를 나눠줘.
```

```
C열의 "지역"을 기준으로 분류하도록 수정해줘.
분류 기준 열을 매크로 실행 시 물어보는 것도 좋겠어.
```

## 실습 4: 조건에 맞는 데이터 추출

### 이런 상황에 씁니다

- 특정 조건에 맞는 데이터만 뽑아서 보고해야 한다
- 고급 필터를 쓰기 귀찮다
- 여러 조건을 조합해서 추출해야 한다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
조건에 맞는 데이터만 추출해서 새 시트에 넣는 매크로.

원본 시트: "데이터"

A열: 이름
B열: 부서
C열: 금액
D열: 날짜

추출 조건:

C열(금액)이 100만원 이상
D열(날짜)이 이번 달 (2026년 3월)

결과:

"추출결과"라는 새 시트에 결과 넣기 (이미 있으면 덮어쓰기)
헤더 포함
추출된 행 수를 메시지로 표시
금액 열에 원화 서식 적용 (₩1,000,000)
결과 시트에 테이블 서식도 적용해줘
```

### 실행 후 확인

- "추출결과" 시트가 생성되었다
- 금액 100만원 이상인 데이터만 들어있다
- 이번 달 데이터만 들어있다
- 원화 서식과 테이블 서식이 적용되었다
- 추출 건수가 메시지로 표시된다

### 응용: 조건을 사용자가 입력

```
위 코드를 수정해줘.
매크로 실행 시 InputBox로 아래 조건을 물어봐:
1. "최소 금액을 입력하세요 (예: 1000000)"
2. "추출할 부서를 입력하세요 (전체는 빈칸)"
```

이렇게 하면 매번 코드를 수정하지 않고도 다양한 조건으로 추출할 수 있습니다.

## 실습 5: 전화번호 형식 통일

### 이런 상황에 씁니다

- 고객 데이터의 전화번호 형식이 제각각이다
- 문자 발송 시 형식이 통일되어야 한다
- 수동으로 하나씩 바꾸기엔 데이터가 너무 많다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
E열의 전화번호를 모두 "010-1234-5678" 형식으로 통일하는 매크로.

처리해야 할 입력 형식들:

01012345678 (붙여쓰기)
010 1234 5678 (공백 구분)
010.1234.5678 (점 구분)
+82-10-1234-5678 (국가번호 포함)
+821012345678
10-1234-5678 (앞의 0 누락)

처리 규칙:

숫자만 추출한 후 010-XXXX-XXXX 형식으로 변환
국가번호 82가 있으면 제거하고 010으로 시작
앞의 0이 누락된 경우 0을 추가
변환 불가능한 형식은 빨간색으로 표시하고 건너뛰기
처리 결과 메시지: "변환: X개, 실패: Y개"
```

### 실행 후 확인

- 다양한 형식의 전화번호가 모두 010-XXXX-XXXX로 변환되었다
- 변환 불가능한 번호는 빨간색으로 표시되었다
- 변환/실패 건수가 메시지로 표시된다
- 헤더 행은 변경되지 않았다

### 응용: 다른 데이터 형식 통일

같은 패턴으로 다른 데이터도 정리할 수 있습니다:

```
이메일 주소를 소문자로 통일하고, 형식이 잘못된 것은 빨간색 표시해줘.
```

```
날짜 형식을 모두 "2026-03-09"로 통일해줘.
"3/9/2026", "2026.03.09", "26-03-09" 등 다양한 형식이 있어.
```

## 직접 해보기: 내 업무에 적용

위 5가지 매크로를 배웠으니, 이제 여러분의 실제 업무 데이터에 적용해봅시다.

### 프롬프트 템플릿

```
엑셀 VBA 코드 만들어줘.
[내 업무에서 반복하는 데이터 정리 작업을 구체적으로 설명]

내 엑셀 파일 구조:

시트 이름: [실제 시트 이름]
A열: [내용], B열: [내용], C열: [내용]
데이터는 2행부터 시작 (1행은 헤더)

원하는 결과:

[구체적인 결과 설명]
```

### 좋은 프롬프트의 핵심

1. 시트 이름을 정확히 알려주세요
2. 열 구성을 설명하세요 (A열은 뭐, B열은 뭐)
3. 원하는 결과를 구체적으로 설명하세요
4. 예외 처리를 요청하세요 (빈 셀은 어떻게, 에러는 어떻게)

## 이번 챕터 핵심 정리

| 매크로 | 용도 | Sub 이름 |
|--------|------|----------|
| 중복 제거 | 중복 데이터 찾기/삭제 | FindDuplicates |
| 빈 행 정리 | 빈 행 삭제 + 공백 제거 | CleanData |
| 부서별 분류 | 특정 열 기준으로 시트 분리 | SplitByDepartment |
| 조건 추출 | 조건에 맞는 데이터만 추출 | ExtractData |
| 형식 통일 | 전화번호 등 형식 표준화 | FormatPhoneNumbers |

- 5가지 매크로를 모두 만들어봤다
- 연습용 데이터로 테스트했다
- 내 실제 업무 데이터에 하나 이상 적용해봤다
- 에러 발생 시 클코에게 에러 메시지를 포함해서 수정 요청하는 방법을 안다

다음 챕터에서는 보고서를 자동으로 생성하는 매크로를 만듭니다.

요약 → 차트 → PDF까지 한번에 처리하는 파이프라인을 구축합니다.
