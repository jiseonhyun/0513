# 엑셀 자동화의 세계 — 클코에게 VBA 시키기.

> CHAPTER 21 / 27 · PART 4 · VBA 업무 자동화 · 예상 시간 30분

## 이번 챕터의 목표

엑셀에 내장된 프로그래밍 언어 VBA를 이해하고,
클코에게 VBA 코드를 만들어달라는 워크플로우를 익힙니다.

직접 코드를 읽을 필요 없습니다. 뭘 하고 싶은지만 설명하면 됩니다.

여기서부터는 Part 4 — 웹 개발과 전혀 다른 세계입니다.

Part 2~3에서는 웹사이트를 만들었지만, Part 4에서는 매일 반복하는 사무 업무를 자동화합니다.

혹시 이런 경험 있으신가요?

매달 엑셀 보고서를 만들면서 "이 작업, 매번 똑같은데 왜 내가 매번 해야 하지?" 하고 생각한 적.

VBA 자동화는 바로 그 문제를 해결합니다.

## VBA란?

VBA는 Visual Basic for Applications의 약자입니다.

쉽게 말하면 엑셀 안에 들어있는 프로그래밍 언어예요.

"프로그래밍"이라고 겁먹을 필요 없습니다.

우리는 코드를 직접 작성하지 않습니다. 클코가 대신 써줍니다.

VBA로 할 수 있는 것들:

- 매일 반복하는 서식 작업을 버튼 하나로 끝내기
- 여러 시트의 데이터를 자동으로 합치기
- 보고서를 자동 생성하고 PDF로 내보내기
- 폴더 안의 엑셀 파일을 한번에 처리하기

VBA는 1993년부터 엑셀에 내장되어 있었습니다.

30년 넘게 사무실에서 쓰이는 검증된 도구예요.

다만 **프로그래밍**이라는 벽 때문에 활용하는 사람이 적었을 뿐입니다.

이제 클코가 그 벽을 없애줍니다.

## 이런 분들에게 필요합니다

아래 중 하나라도 해당되면 이 챕터가 도움이 됩니다:

- 매일 같은 양식의 보고서를 손으로 만든다
- 여러 엑셀 파일에서 데이터를 복사/붙여넣기 한다
- 데이터를 정리하고 정렬하는 작업이 반복된다
- 숫자에 쉼표 넣기, 서식 맞추기를 매번 수동으로 한다
- "이걸 왜 매번 수동으로 해야 하지?" 하고 생각한 적 있다
- 엑셀 함수(VLOOKUP, IF 등)만으로는 한계를 느낀다

하나라도 체크했다면, VBA 자동화로 매일 30분~1시간을 아낄 수 있습니다.

## 클코 + VBA 워크플로우

핵심 흐름은 이렇습니다:

```
① 내가 할 일을 클코에게 설명
② 클코가 VBA 코드를 작성해줌
③ 코드를 복사해서 엑셀 VBA 에디터에 붙여넣기
④ 실행 (F5 또는 버튼 클릭)
```

중요한 포인트: 클코는 여러분의 엑셀 파일을 직접 열거나 조작할 수 없습니다.

대신 VBA 코드를 텍스트로 만들어줍니다. 여러분은 그 코드를 엑셀에 복사해서 실행하면 됩니다.

마치 요리사에게 레시피를 받아서 직접 요리하는 것과 같습니다.

레시피(코드)를 이해할 필요 없이, 순서대로 따라하면 됩니다.

## 사전 준비: 개발 도구 탭 활성화 (최초 1회)

엑셀에서 VBA를 쓰려면 **개발 도구** 탭을 켜야 합니다.

한 번만 설정하면 됩니다.

### 설정 방법

1. 엑셀 상단 메뉴에서 파일 클릭
2. 왼쪽 하단의 옵션 클릭
3. 리본 사용자 지정 클릭
4. 오른쪽 목록에서 개발 도구에 체크 표시
5. 확인 클릭

설정이 완료되면 엑셀 상단에 개발 도구 탭이 나타납니다.

### 확인 체크리스트

- 엑셀 상단에 "개발 도구" 탭이 보인다
- "개발 도구" 탭을 클릭하면 "Visual Basic", "매크로" 등의 버튼이 보인다

## VBA 에디터 열기

VBA 에디터는 엑셀 안에 숨어있는 코드 작성 공간입니다.

여는 방법은 두 가지입니다:

### 방법 1: 단축키 (추천)

```
Alt + F11
```

이것만 기억하세요. 앞으로 수십 번 쓸 겁니다.

### 방법 2: 메뉴

1. 개발 도구 탭 클릭
2. Visual Basic 버튼 클릭

둘 다 같은 VBA 에디터가 열립니다.

### VBA 에디터 화면 구성

VBA 에디터가 열리면 이런 화면이 나타납니다:

- 왼쪽: 프로젝트 탐색기 (파일/시트 목록)
- 가운데: 코드 창 (여기에 코드를 붙여넣습니다)
- 아래: 즉시 실행 창 (디버깅용, 지금은 무시)

가운데 코드 창이 비어있다면, 메뉴에서 삽입 → 모듈을 클릭하세요.

하얀 코드 창이 나타납니다. 여기에 클코가 준 코드를 붙여넣으면 됩니다.

## 첫 번째 테스트: Hello World 매크로

프로그래밍에서는 전통적으로 첫 번째 프로그램을 **Hello World**로 만듭니다.

우리도 해봅시다.

### Step 1: 클코에게 코드 요청

터미널에서 클코에게 이렇게 말하세요:

```
엑셀 VBA 코드 만들어줘.
A1 셀에 "안녕하세요! VBA 자동화 시작!"을 입력하는 매크로.
코드만 주면 돼. 내가 엑셀 VBA 에디터에 복사해서 쓸 거야.
```

클코가 이런 코드를 줄 겁니다:

```vba
Sub HelloWorld()
    Range("A1").Value = "안녕하세요! VBA 자동화 시작!"
    MsgBox "완료! A1 셀을 확인하세요.", vbInformation, "알림"
End Sub
```

### 코드 블록만 정확하게 복사하는 법

클코의 출력에는 설명 텍스트와 코드가 섞여 있습니다. 코드 블록만 복사해야 합니다.

방법 1: 터미널에서 직접 선택

1. 코드가 시작되는 `Sub HelloWorld()` 부분에 마우스 커서를 놓고
2. 마우스 왼쪽 버튼을 누른 채로 `End Sub`까지 드래그
3. Ctrl + C로 복사

방법 2: 클코에게 코드만 달라고 요청

```
코드만 다시 보여줘. 설명 없이 코드만.
```

주의: 코드 앞뒤의 ` ```vba `이나 ` ``` ` 표시는 복사하지 마세요! 이것은 "여기부터 코드입니다"라는 표시일 뿐, 코드가 아닙니다. `Sub`로 시작해서 `End Sub`으로 끝나는 부분만 복사하세요.

### Step 2: 엑셀에 코드 붙여넣기

1. 엑셀에서 `Alt + F11` 눌러 VBA 에디터 열기
2. 메뉴에서 삽입 → 모듈 클릭
3. 새로 나타난 코드 창에 클코가 준 코드를 Ctrl + V로 붙여넣기

### Step 3: 실행

1. 코드 창에서 아무 곳이나 클릭 (커서를 코드 안에 위치)
2. F5 키를 누르거나 상단의 ▶ (실행) 버튼 클릭
3. "완료!" 메시지가 뜨면 성공!
4. 엑셀로 돌아가서 A1 셀을 확인하세요

### 확인 체크리스트

- `Alt + F11`로 VBA 에디터가 열린다
- 모듈을 삽입하고 코드를 붙여넣을 수 있다
- F5로 매크로가 실행된다
- A1 셀에 텍스트가 입력되었다

## 파일 저장: .xlsm으로 저장하세요!

이것은 매우 중요합니다.

VBA 코드가 포함된 파일은 반드시 `.xlsm` 형식으로 저장해야 합니다.

일반 `.xlsx`로 저장하면 매크로가 전부 사라집니다.

### 저장 방법

1. 파일 → 다른 이름으로 저장
2. 파일 형식을 "Excel 매크로 사용 통합 문서 (*.xlsm)" 으로 변경
3. 저장

### 자주 하는 실수

| 실수 | 결과 |
|------|------|
| .xlsx로 저장 | 매크로 전부 삭제됨 |
| 저장할 때 "매크로 없이 저장" 선택 | 매크로 삭제됨 |
| .xlsm으로 저장 안 하고 엑셀 종료 | 매크로 사라짐 |

팁: 파일명에 "매크로"를 넣어두면 헷갈리지 않습니다.

예: `월간보고서_매크로.xlsm`

## 보안 경고 처리

`.xlsm` 파일을 열면 상단에 노란 보안 경고가 뜹니다:

```
보안 경고: 매크로를 사용할 수 없도록 설정했습니다.  [콘텐츠 사용]
```

**콘텐츠 사용** 버튼을 클릭해야 매크로가 실행됩니다.

이건 정상적인 보안 기능이에요. 모르는 사람이 보낸 파일의 매크로는 실행하지 마세요.

본인이 만든 매크로 파일만 "콘텐츠 사용"을 클릭하면 안전합니다.

## 클코에게 VBA를 잘 시키는 프롬프트 팁

### 기본 템플릿

```
엑셀 VBA 코드 만들어줘.
[구체적인 작업 설명]
코드만 주면 돼. 내가 엑셀 VBA 에디터에 복사해서 쓸 거야.
```

### 좋은 프롬프트 예시

```
엑셀 VBA 코드 만들어줘.
"매출데이터" 시트의 A열~D열 데이터를 부서별로 합계를 구해서
"요약" 시트에 표로 정리하는 매크로.
A열은 날짜, B열은 부서명, C열은 항목, D열은 금액이야.
코드만 주면 돼.
```

### 프롬프트를 잘 쓰는 5가지 원칙

1. 시트 이름을 알려주세요: "Sheet1"이 아니라 "매출데이터"처럼 실제 이름
2. 열 구성을 설명하세요: "A열은 이름, B열은 부서, C열은 금액"
3. 원하는 결과를 구체적으로: "합계를 구해줘"가 아니라 "부서별 합계를 새 시트에 표로 만들어줘"
4. 예외 상황도 알려주세요: "빈 셀은 건너뛰고" "헤더 행은 제외하고"
5. 에러 처리를 요청하세요: "잘못되면 메시지로 알려줘"

### 코드가 잘 안 될 때

클코에게 이렇게 다시 요청하세요:

```
방금 준 VBA 코드를 실행했더니 이런 에러가 나와:
[에러 메시지를 그대로 복사]

내 엑셀 파일 상황:

시트 이름: "데이터"
A열에 이름, B열에 금액
데이터는 2행부터 100행까지
```

에러 메시지를 그대로 복사하는 것이 핵심입니다.

클코는 에러 메시지를 보면 정확히 뭐가 문제인지 알 수 있습니다.

## 자주 발생하는 에러와 해결법

| 에러 | 원인 | 해결 |
|------|------|------|
| "Sub 또는 Function이 정의되지 않았습니다" | 매크로 이름이 잘못됨 | 모듈에 코드가 제대로 붙여넣어졌는지 확인 |
| "실행 시간 오류 '9': 아래 첨자 사용이 잘못되었습니다" | 시트 이름이 다름 | 실제 시트 이름과 코드의 시트 이름이 같은지 확인 |
| "실행 시간 오류 '1004'" | 셀 범위가 잘못됨 | 클코에게 에러 메시지와 함께 재요청 |
| 매크로 목록에 아무것도 안 보임 | 모듈에 코드가 없음 | 삽입 → 모듈 후 코드 붙여넣기 |
| "매크로가 사용할 수 없도록 설정되었습니다" | 보안 설정 | 파일 상단의 "콘텐츠 사용" 클릭 |

## 이번 챕터 핵심 정리

- VBA는 엑셀에 내장된 프로그래밍 언어 — 우리는 코드를 직접 안 씀
- 워크플로우: 클코에게 설명 → 코드 받기 → 엑셀에 붙여넣기 → 실행
- VBA 에디터: `Alt + F11`
- 파일 저장: 반드시 `.xlsm` 형식
- 보안 경고: "콘텐츠 사용" 클릭
- 프롬프트: 시트 이름, 열 구성, 원하는 결과를 구체적으로

다음 챕터에서는 실제로 업무에 바로 쓸 수 있는 매크로들을 만들어봅니다.
