# 모르는 기술도 OK — Context7 MCP.

> CHAPTER 16 / 27 · PART 3 · 고구마마켓 · 예상 시간 30분

## 이번 챕터의 목표

클코가 최신 기술 문서를 실시간으로 읽을 수 있게 해주는 Context7 MCP를 설정합니다.

8장에서 첫 MCP(Supabase)를 연결했던 거 기억하시죠?

"MCP = 클코에게 손을 달아주는 것"이라고 배웠습니다.

이번에는 클코에게 최신 교과서를 펼쳐보는 능력을 달아줍니다.

설정이 매우 간단합니다. 명령어 한 줄, 가입도 필요 없고, 무료입니다.

## 문제: 클코도 모르는 게 있다

클코는 매우 똑똑하지만, 학습 데이터에 시간 제한이 있습니다.

예를 들어:

- 토스페이먼츠 API가 지난달에 바뀌었다면? → 클코는 옛날 방식으로 코드를 짬
- Supabase가 새 기능을 추가했다면? → 클코는 새 기능의 존재를 모름
- 새로 나온 라이브러리를 쓰고 싶다면? → 클코의 학습 데이터에 아예 없음

이런 일이 실제로 자주 발생합니다:

```
나: "토스페이먼츠 결제 위젯 연동해줘"
클코: (1년 전 문서 기반으로 코드 작성)
→ 에러! API가 바뀌었음...
→ 또 에러! 함수 이름이 바뀌었음...
→ 3번째 시도... 또 에러!
```

이 상황에서 필요한 것이 Context7 MCP입니다.

## Context7이란?

Context7은 라이브러리/프레임워크의 최신 공식 문서를 실시간으로 가져오는 MCP 도구입니다.

### 비유로 이해하기

| 상황 | Context7 없이 | Context7 있으면 |
|------|--------------|----------------|
| 토스 결제 연동 | 1년 전 기억으로 코딩 | 토스 최신 문서 보고 코딩 |
| 새 라이브러리 사용 | "모르겠어요" | 공식 문서 읽고 바로 적용 |
| 에러 해결 | 여러 번 시행착오 | 정확한 최신 사용법 확인 |

쉽게 말해, Context7은 클코에게 **최신 교과서를 펼쳐보는 능력**을 주는 것입니다.

시험 볼 때 교과서를 펼쳐보고 답을 쓸 수 있다면, 당연히 더 정확하겠죠?

## Context7 MCP 설정하기

### 1단계: 클로드코드 종료

현재 클로드코드가 실행 중이라면 `/exit`로 종료합니다.

### 2단계: MCP 추가

터미널(PowerShell)에서 아래 명령어를 입력합니다:

```
claude mcp add context7 -- npx -y @upstash/context7-mcp@latest
```

이게 전부입니다. 별도의 키나 가입이 필요 없습니다.

### 3단계: 클로드코드 재시작

```
claude
```

### 4단계: 연결 확인

```
Context7 MCP 연결됐는지 확인해줘
```

클코가 Context7 도구를 사용할 수 있다고 응답하면 성공입니다.

### 안 되면?

| 증상 | 해결 |
|------|------|
| "MCP server failed to start" | Node.js가 설치되어 있는지 확인. node --version으로 확인 |
| npx 관련 에러 | npm install -g npx 실행 후 다시 시도 |
| 연결은 됐는데 사용 안 됨 | 클로드코드를 완전히 종료 후 재시작 |

## 사용법

Context7이 설정되면, 클코에게 "최신 문서 보고" 라는 말을 붙이면 됩니다.

### 예시 1: 토스페이먼츠 문서 확인

```
토스페이먼츠 최신 문서를 보고, 결제 위젯 연동 코드 만들어줘
```

클코가 Context7을 통해 토스페이먼츠의 가장 최신 공식 문서를 읽고, 그 문서에 맞는 코드를 작성합니다.

### 예시 2: Supabase 최신 API 확인

```
Supabase Auth 최신 문서 확인하고, 소셜 로그인 코드 검증해줘.
지금 코드가 최신 방식이 맞는지 확인.
```

### 예시 3: Next.js 업데이트 대응

```
Next.js App Router 최신 문서 보고, 우리 프로젝트가 최신 방식을 따르고 있는지 확인해줘
```

### 예시 4: 처음 쓰는 라이브러리

```
react-hook-form 최신 문서 보고, 상품 등록 폼에 유효성 검사 추가해줘.
가격은 숫자만, 상품명은 2글자 이상 필수.
```

### 예시 5: 에러 해결

```
이 에러가 계속 나는데, Supabase 최신 문서 확인하고 해결해줘:
[에러 메시지]
```

## 언제 쓰면 좋은가?

### 반드시 쓰세요

- 새로운 라이브러리/서비스를 처음 연동할 때 — 최신 사용법 확인
- 같은 에러가 3번 이상 반복될 때 — 문서가 바뀌었을 가능성
- "이 함수/메서드가 없다"는 에러가 날 때 — API가 변경되었을 가능성

### 안 써도 되는 경우

- HTML, CSS 같은 기본 기술 — 거의 안 바뀜
- 이미 잘 동작하는 코드 수정 — 문서 확인할 필요 없음
- 단순한 UI 변경 — 라이브러리 문서와 무관

## Context7이 지원하는 문서들

Context7은 수천 개의 라이브러리/프레임워크 문서를 지원합니다. 몇 가지 예:

| 분류 | 라이브러리 예시 |
|------|--------------|
| 프레임워크 | Next.js, React, Vue, Svelte |
| 데이터베이스 | Supabase, Firebase, Prisma |
| 결제 | 토스페이먼츠, Stripe |
| UI | Tailwind CSS, shadcn/ui |
| 인증 | NextAuth, Clerk |
| 기타 | Vercel, Resend, 등등 |

## 다음 챕터 준비

다음 챕터에서는 토스페이먼츠 결제 연동을 합니다.

토스페이먼츠는 API가 자주 업데이트되기 때문에, 방금 설정한 Context7이 특히 유용합니다. Context7이 최신 토스 문서를 읽고, 정확한 코드를 만들어 줄 겁니다.

```
토스페이먼츠 최신 문서를 보고 결제 연동해줘
```

이 한 줄이면, 클코가 알아서 최신 토스 문서를 읽고 코드를 작성합니다. Context7이 없었다면 에러 수정에 시간을 많이 쓸 수도 있었을 겁니다.

## 핵심 정리

- 클코의 지식에는 시간 제한이 있다 — 최신 변경사항을 모를 수 있음
- Context7 MCP = 클코에게 최신 공식 문서를 실시간으로 읽는 능력을 줌
- 설정은 한 줄 명령어로 끝 (키 필요 없음, 무료)
- 사용법: 프롬프트에 "최신 문서 보고" 를 붙이면 됨
- 새 기술 연동, 반복 에러, API 변경 시 특히 유용
- 한번 설정하면 모든 프로젝트에서 계속 사용 가능
