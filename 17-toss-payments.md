# 토스페이먼츠 결제 연동.

> CHAPTER 17 / 27 · PART 3 · 고구마마켓 · 예상 시간 40분

## 이번 챕터의 목표

고구마마켓에 실제 결제 기능을 붙여봅니다.

테스트 모드로 진행하므로 실제 돈이 빠지지 않습니다.

"결제"라는 단어만 들으면 겁이 날 수 있습니다.

"결제 연동이 잘못되면 돈이 이상하게 빠지는 거 아니야?"

걱정 마세요. 토스페이먼츠는 테스트 모드를 제공합니다.

테스트 모드에서는 절대로 실제 결제가 이루어지지 않습니다.

가짜 카드번호로 결제 연습을 해볼 수 있습니다.

이번 챕터도 15장(소셜 로그인)처럼 외부 서비스 설정이 필요합니다.

하지만 토스페이먼츠는 한국어 문서에 화면도 한국어라 훨씬 편합니다.

## 토스페이먼츠란?

토스페이먼츠는 온라인 결제를 쉽게 붙일 수 있게 해주는 서비스입니다.

여러분이 자주 쓰는 온라인 쇼핑몰에서 "결제하기"를 누르면 나오는 그 화면 — 카드 결제, 계좌이체, 카카오페이 등을 선택하는 화면 — 을 만들어주는 서비스입니다.

### 왜 토스페이먼츠인가?

| 결제 서비스 | 특징 |
|-----------|------|
| 토스페이먼츠 | 한국 서비스, 문서가 한국어, 테스트 쉬움 |
| Stripe | 해외 서비스, 영어 문서, 글로벌 서비스에 적합 |
| 아임포트 | 여러 PG사 통합, 설정이 복잡 |

한국에서 서비스를 만든다면 토스페이먼츠가 가장 편합니다.

## 토스페이먼츠 가입

### 1단계: 사이트 접속

```
https://developers.tosspayments.com
```

### 2단계: 회원가입

1. 회원가입 클릭
2. 이메일로 가입 (또는 토스 계정 연동)
3. 가입 완료 후 로그인

### 3단계: 테스트 키 확인

1. 로그인 후 개발 정보 페이지로 이동
2. 테스트 탭 선택 (실연동이 아닌 테스트)
3. 두 가지 키를 복사합니다:

| 키 | 형식 | 용도 |
|----|------|------|
| 클라이언트 키 | test_ck_ 으로 시작 | 브라우저에서 결제 위젯 표시 |
| 시크릿 키 | test_sk_ 으로 시작 | 서버에서 결제 확인(confirm) |

중요: 시크릿 키(`test_sk_`)는 절대 외부에 공개하면 안 됩니다!

클라이언트 키(`test_ck_`)는 브라우저에 노출되어도 괜찮습니다.

## 환경변수 설정

클코에게 키를 알려줘야 합니다. 환경변수 파일에 저장합니다:

```
프로젝트의 .env.local 파일에 토스페이먼츠 키를 추가해줘:
```

`NEXT_PUBLIC_`으로 시작하는 변수는 브라우저에서도 접근 가능합니다.

시크릿 키는 `NEXT_PUBLIC_` 없이 저장해서 서버에서만 사용합니다.

## 결제 흐름 이해하기

결제가 어떻게 진행되는지 먼저 이해하고 갑시다:

```
1. 사용자가 "구매하기" 버튼 클릭
          ↓
2. 토스 결제 위젯이 나타남 (카드번호 입력 화면)
          ↓
3. 사용자가 카드 정보 입력하고 결제
          ↓
4. 토스가 우리 서버에 "결제 됐어!" 알려줌
          ↓
5. 우리 서버에서 토스에 "진짜야?" 확인 (confirm)
          ↓
6. 확인 완료 → 결제 완료 페이지 표시
```

왜 5번(confirm)이 필요한가요? 보안 때문입니다. 누군가 가짜 결제 요청을 보낼 수 있으니, 서버에서 토스에 직접 확인하는 과정이 필요합니다.

## 클코에게 결제 기능 시키기

이제 Context7이 설치되어 있으니, 최신 토스 문서를 참고하게 합니다:

```
고구마마켓에 토스페이먼츠 결제를 연동해줘.
토스페이먼츠 최신 문서를 참고해서 만들어줘.

요구사항:

상품 상세 페이지에 "구매하기" 버튼 추가
버튼 누르면 토스 결제 위젯(Payment Widget) 표시
결제 완료 후 완료 페이지로 이동
서버에서 결제 확인(confirm) API 구현
테스트 모드로 설정

환경변수:

NEXT_PUBLIC_TOSS_CLIENT_KEY (클라이언트 키)
TOSS_SECRET_KEY (시크릿 키)
이미 .env.local에 있음

결제 완료 페이지에 보여줄 것:
```

포인트: "토스페이먼츠 최신 문서를 참고해서"라고 쓰면, Context7이 자동으로 토스 최신 문서를 가져옵니다.

## 테스트 결제 해보기

결제 기능이 만들어졌으면 테스트해봅니다.

### 테스트용 카드 정보

토스 테스트 모드에서는 가짜 카드 정보로 결제할 수 있습니다:

| 항목 | 값 |
|------|-----|
| 카드번호 | 4330-0000-0000-0001 |
| 유효기간 | 아무 미래 날짜 (예: 12/28) |
| CVC | 아무 3자리 숫자 (예: 123) |
| 비밀번호 앞 2자리 | 아무 2자리 (예: 12) |

안심하세요: 테스트 모드에서는 절대로 실제 결제가 이루어지지 않습니다.

테스트 키(`test_ck_`, `test_sk_`)를 사용하는 한 100% 안전합니다.

### 테스트 순서

1. `npm run dev`로 개발 서버 실행
2. 아무 상품 상세 페이지로 이동
3. "구매하기" 버튼 클릭
4. 결제 위젯에서 "카드" 선택
5. 위의 테스트 카드 정보 입력
6. 결제 진행
7. 완료 페이지가 나오면 성공!

## 안 되면?

| 증상 | 원인 | 해결 |
|------|------|------|
| 결제 위젯이 안 뜸 | 클라이언트 키가 잘못됨 | .env.local에서 NEXT_PUBLIC_TOSS_CLIENT_KEY 확인 |
| 결제 위젯은 뜨는데 결제 실패 | 시크릿 키가 잘못됨 | .env.local에서 TOSS_SECRET_KEY 확인 |
| "클라이언트 키를 확인해주세요" | 키 앞뒤에 공백이 있음 | .env.local에서 공백 제거 |
| 환경변수 변경이 안 먹힘 | 서버 재시작 필요 | 개발 서버를 종료하고 다시 npm run dev |
| confirm API에서 에러 | API 버전 불일치 | "토스페이먼츠 최신 문서 보고 confirm API 수정해줘" |
| 하얀 빈 화면 | JavaScript 에러 | F12 → Console 탭 확인 후 클코에게 전달 |

### 그래도 안 되면

```
토스 결제 연동이 안 되고 있어.
토스페이먼츠 최신 문서를 다시 확인하고, 처음부터 다시 만들어줘.
현재 에러:
[에러 내용이나 스크린샷]
```

Context7 덕분에 최신 문서를 기반으로 다시 만들어주므로, 보통 이 방법으로 해결됩니다.

## 확인 체크리스트

- 상품 상세 페이지에 "구매하기" 버튼이 있는가?
- 버튼을 누르면 토스 결제 위젯이 나타나는가?
- 카드 결제를 선택할 수 있는가?
- 테스트 카드 정보로 결제가 진행되는가?
- 결제 완료 후 완료 페이지가 나오는가?
- 완료 페이지에 주문 번호, 금액이 표시되는가?
- 다시 메인 페이지로 돌아갈 수 있는가?

## 핵심 정리

- 토스페이먼츠 = 온라인 결제를 쉽게 붙이는 서비스
- 테스트 모드 = 실제 돈이 안 빠지는 연습 모드 (키가 `test_`로 시작)
- 클라이언트 키 = 결제 화면 표시용 (브라우저에서 사용)
- 시크릿 키 = 결제 확인용 (서버에서만 사용, 절대 공개 금지)
- 결제 흐름: 구매 클릭 → 결제 위젯 → 결제 → 서버 확인(confirm) → 완료
- Context7 덕분에 최신 토스 문서를 기반으로 정확한 코드 생성 가능
- 나중에 실제 서비스로 전환하려면 실연동 키로 교체하면 됨
