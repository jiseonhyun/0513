# 내가 안 눌러도 테스트 — Playwright MCP.

> CHAPTER 18 / 27 · PART 3 · 고구마마켓 · 예상 시간 40분

## 이번 챕터의 목표

클코가 직접 브라우저를 열고 클릭하고 확인할 수 있게 해주는
Playwright MCP를 설정하고 활용합니다.

이번이 세 번째 MCP입니다.

8장에서 Supabase MCP(DB 조작), 16장에서 Context7 MCP(최신 문서)를 연결했습니다.

이번에는 클코에게 **눈(브라우저)**을 달아줍니다.

설정은 역시 명령어 한 줄이면 끝납니다.

## 문제: 기능이 늘어날수록 테스트가 힘들다

고구마마켓이 점점 커지고 있습니다. 지금까지 만든 기능을 정리하면:

- 상품 목록 보기
- 상품 등록
- 상품 수정
- 상품 삭제
- 구글/카카오 로그인
- 로그인 사용자만 등록 가능
- 본인 상품만 수정/삭제
- 토스 결제

새 기능을 추가할 때마다 확인해야 할 것:

```
1. 메인 페이지 잘 열리나? ✓
2. 상품 카드 클릭하면 상세 페이지 가나? ✓
3. 상품 등록 잘 되나? ✓
4. 수정 잘 되나? ✓
5. 삭제 잘 되나? ✓
6. 로그인/로그아웃 잘 되나? ✓
7. 결제 잘 되나? ✓
8. 모바일에서도 잘 보이나? ✓
```

매번 이걸 다 일일이 클릭해서 확인한다고요? 기능이 10개만 넘어가도 테스트에 30분이 걸립니다. 게다가 사람은 실수로 빠뜨리기도 합니다.

여기서 Playwright MCP가 등장합니다.

## Playwright란?

Playwright는 브라우저를 자동으로 조작하는 도구입니다.

사람이 하는 것처럼:

- 페이지를 열고
- 버튼을 클릭하고
- 글자를 입력하고
- 화면에 뭐가 보이는지 확인하고
- 스크린샷도 찍을 수 있습니다

이걸 MCP로 클코에 연결하면, 클코가 직접 브라우저를 열고 위 동작을 수행합니다.

### 비유

| 상황 | MCP 없이 | Playwright MCP 있으면 |
|------|---------|----------------------|
| 테스트 | 내가 직접 클릭하면서 확인 | 클코가 브라우저 열고 확인 |
| 스크린샷 | 내가 직접 찍어서 전달 | 클코가 알아서 찍고 분석 |
| 모바일 테스트 | 브라우저 크기 직접 조절 | 클코가 자동으로 크기 변경 |
| 에러 확인 | 내가 F12 열어서 확인 | 클코가 콘솔 에러 자동 수집 |

## Playwright MCP 설정하기

### 1단계: 클로드코드 종료

현재 클로드코드가 실행 중이라면 `/exit`로 종료합니다.

### 2단계: MCP 추가

터미널(PowerShell)에서 아래 명령어를 입력합니다:

```
claude mcp add playwright -- npx -y @anthropic-ai/mcp-server-playwright
```

### 3단계: 클로드코드 재시작

```
claude
```

### 4단계: 연결 확인

```
Playwright MCP 연결됐는지 확인해줘
```

### 안 되면?

| 증상 | 해결 |
|------|------|
| "MCP server failed to start" | Node.js 버전 확인. 18 이상 필요 (node --version) |
| 브라우저가 안 열림 | "Playwright 브라우저 설치해줘" 요청 |
| 타임아웃 에러 | 개발 서버(npm run dev)가 먼저 실행 중인지 확인 |

## 사용법 1: 기능 테스트

개발 서버를 먼저 실행해야 합니다. 터미널을 하나 더 열어서 개발 서버를 실행합니다.

### 터미널을 하나 더 여는 법

클코가 돌아가는 터미널과 별도의 터미널이 필요합니다.

방법 1: VS Code에서 터미널 추가

1. VS Code 하단 터미널 패널에서 + 버튼 클릭 (터미널 패널 오른쪽 상단)
2. 새 터미널이 열림
3. 탭으로 기존 터미널(클코)과 새 터미널을 오갈 수 있음

방법 2: 단축키

- Ctrl + Shift + ` (백틱) → 새 터미널 생성

방법 3: PowerShell을 따로 열기

- Windows 키 → "PowerShell" 검색 → 실행
- `cd C:\claude-projects\goguma-market` 입력

새 터미널에서:

```
npm run dev
```

그리고 클코에게:

```
Playwright로 고구마마켓 테스트해줘.
localhost:3000에서:
```

클코가 실제로 브라우저를 열고 위 동작을 하나씩 수행합니다. 각 단계의 결과를 알려줍니다:

```
✅ 메인 페이지: 상품 목록 3개 표시됨
✅ 상품 "고구마" 클릭 → 상세 페이지 이동 성공
✅ 상세 페이지: 상품명, 가격(3,000원), 설명 모두 표시
✅ 뒤로가기 → 목록 페이지 복귀 성공
```

## 사용법 2: 스크린샷

디자인을 확인하고 싶을 때 유용합니다.

### PC 화면 스크린샷

```
localhost:3000 메인 페이지 스크린샷 찍어서 보여줘
```

### 모바일 화면 스크린샷

```
브라우저를 모바일 사이즈(375x667)로 변경하고
localhost:3000 메인 페이지 스크린샷 찍어줘
```

### 여러 페이지 한번에

```
다음 페이지들 스크린샷 찍어줘:
1. 메인 페이지
2. 상품 상세 페이지
3. 상품 등록 페이지
```

## 사용법 3: 에러 재현

"어떤 순서로 했더니 에러가 났는데, 다시 재현이 안 돼"라는 상황에서:

```
Playwright로 이 순서대로 해보고 에러가 나는지 확인해줘:
1. localhost:3000 열기
2. 로그인 버튼 클릭
3. 상품 등록 페이지로 이동
4. 상품명만 입력하고 (가격은 비우고) 등록 버튼 클릭
5. 어떤 에러가 나는지 확인
```

## 사용법 4: CRUD 전체 테스트

새 기능 추가 후, 기존 기능이 안 깨졌는지 한번에 확인:

```
Playwright로 고구마마켓 전체 기능 테스트해줘:

메인 페이지 열기 → 상품 목록 표시되는지
상품 등록 페이지 → 새 상품 등록 (테스트 상품)
등록한 상품이 목록에 나오는지 확인
등록한 상품 클릭 → 상세 페이지 정상인지
수정 버튼 → 상품명 변경 → 저장 → 반영되는지
삭제 버튼 → 삭제 확인 → 목록에서 사라지는지
```

## 사용법 5: 콘솔 에러 확인

브라우저에서 보이지 않는 에러를 잡아줍니다:

```
Playwright로 localhost:3000의 모든 페이지를 돌면서
브라우저 콘솔에 에러가 없는지 확인해줘.
에러가 있으면 어떤 페이지에서 어떤 에러인지 정리해줘.
```

## 언제 쓰면 좋은가?

| 상황 | 프롬프트 예시 |
|------|-------------|
| 새 기능 추가 후 | "기존 기능 다 잘 되는지 Playwright로 테스트해줘" |
| 디자인 확인 | "모바일 화면 스크린샷 보여줘" |
| 에러 재현 | "이 순서대로 해보고 에러 나는지 확인해줘" |
| 배포 전 최종 점검 | "전체 페이지 돌면서 에러 없는지 확인해줘" |
| 다른 사람에게 보여줄 때 | "메인 페이지 스크린샷 찍어줘" |

## 주의사항

### Playwright는 만능이 아닙니다

- 로그인이 필요한 기능은 테스트하기 어려울 수 있음 (소셜 로그인은 자동화가 복잡)
- 결제 테스트는 토스 위젯 내부 조작이 제한적
- 네트워크 상태에 따라 타임아웃이 날 수 있음

### 개발 서버가 반드시 실행 중이어야 합니다

Playwright는 실제 브라우저로 접속하므로, `npm run dev`가 돌고 있어야 합니다.

```
테스트 전에 먼저:
터미널에서 npm run dev 실행 확인
```

## 핵심 정리

- Playwright MCP = 클코가 직접 브라우저를 열고 클릭하고 확인하는 도구
- 설정: `claude mcp add playwright -- npx -y @anthropic-ai/mcp-server-playwright`
- 기능 테스트: "이 순서대로 해보고 결과 알려줘"
- 스크린샷: "PC/모바일 화면 찍어줘"
- 에러 확인: "전체 페이지 돌면서 콘솔 에러 확인해줘"
- 개발 서버(`npm run dev`)가 먼저 실행 중이어야 함
- 수동 테스트를 완전히 대체하진 못하지만, 반복 작업을 크게 줄여줌
