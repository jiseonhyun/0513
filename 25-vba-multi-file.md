# 여러 파일 한번에 처리하기.

> CHAPTER 25 / 27 · PART 4 · VBA 업무 자동화 · 예상 시간 40분

## 이번 챕터의 목표

폴더 안의 엑셀 파일을 한번에 열어서 처리하는 자동화를 만듭니다.

여러 파일 합치기, 일괄 서식 적용, 시트 분리 저장, 파일 목록 생성까지

4가지 핵심 매크로를 만듭니다.

## 시작하기 전에: 꼭 지켜야 할 안전 수칙

여러 파일을 한번에 처리하는 매크로는 강력하지만 위험할 수도 있습니다.

한 번 잘못 실행하면 여러 파일이 동시에 영향을 받기 때문입니다.

### 반드시 지켜야 할 3가지 규칙

규칙 1: 백업 먼저

```
처리할 폴더를 통째로 복사해서 백업하세요.
폴더 우클릭 → 복사 → 붙여넣기 → "데이터_백업_0309" 같은 이름으로.
```

규칙 2: 파일 닫기

처리할 엑셀 파일이 이미 열려있으면 에러가 납니다.

매크로를 실행하기 전에 처리 대상 파일을 모두 닫으세요.

매크로를 실행하는 파일(코드가 들어있는 파일)만 열어두면 됩니다.

규칙 3: 연습 폴더에서 먼저 테스트

실제 업무 파일에 바로 적용하지 마세요.

테스트용 파일 3~4개를 만들어서 먼저 확인하세요.

### 테스트 파일 만들기

```
엑셀 VBA 코드 만들어줘.
바탕화면에 "테스트폴더"를 만들고,
거기에 엑셀 파일 4개를 생성하는 매크로.

각 파일 구성:

"1월_매출.xlsx": A열 이름, B열 부서, C열 금액 — 10행 샘플 데이터
"2월_매출.xlsx": 같은 구조 — 15행 샘플 데이터
"3월_매출.xlsx": 같은 구조 — 12행 샘플 데이터
"요약.xlsx": 빈 파일 (합친 결과를 여기 넣을 예정)
```

## 실습 1: 여러 파일 데이터 합치기

### 이런 상황에 씁니다

- 매달 부서별로 파일을 받는데 하나로 합쳐야 한다
- 지점별 매출 파일을 통합 보고서로 만들어야 한다
- 여러 사람이 각각 입력한 파일을 하나로 모아야 한다

이 작업을 수동으로 하면: 파일 열기 → 데이터 선택 → 복사 → 다른 파일에 붙여넣기 → 반복...

파일이 10개만 넘어도 30분 이상 걸립니다.

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.

지정한 폴더 안에 있는 모든 엑셀 파일(.xlsx, .xls)을 열어서
각 파일의 데이터를 하나의 시트에 합치는 매크로.
동작 순서:

폴더 선택 다이얼로그 표시
해당 폴더의 모든 엑셀 파일 목록 확인
"X개 파일을 합칩니다. 계속하시겠습니까?" 확인 메시지
각 파일의 첫 번째 시트 데이터를 현재 통합문서의 "통합데이터" 시트에 복사
헤더는 첫 번째 파일 것만 사용 (나머지 파일은 헤더 제외)
맨 앞에 "출처" 열을 추가하여 어떤 파일에서 가져왔는지 파일명 표시

추가 기능:

진행 상황을 상태표시줄에 표시: "처리 중: 3/10 — 3월_매출.xlsx"
빈 파일은 건너뛰기
열 개수가 다른 파일이 있으면 경고 메시지 후 건너뛰기
완료 메시지: "X개 파일, 총 Y행 합침 완료"
```

### 실행 후 확인

- 폴더 선택 다이얼로그가 나타났다
- "통합데이터" 시트에 모든 파일의 데이터가 합쳐졌다
- "출처" 열에 파일명이 표시된다
- 헤더가 한 번만 있다 (중복 헤더 없음)
- 상태표시줄에 진행 상황이 표시되었다
- 합친 행 수가 각 파일 행 수의 합과 일치한다

### 응용: 특정 파일만 합치기

```
위 코드를 수정해줘.
폴더 안의 모든 파일이 아니라,
파일명에 "매출"이 포함된 파일만 합치도록.
```

```
.xlsx 파일만 합치고, .xls 파일은 제외하도록 수정해줘.
```

## 실습 2: 여러 파일에 같은 서식 적용

### 이런 상황에 씁니다

- 여러 파일에 동일한 회사 양식을 적용해야 한다
- 모든 보고서의 헤더, 서식, 인쇄 설정을 통일해야 한다
- 매번 파일을 하나씩 열어서 서식을 바꾸고 있다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.

폴더 안의 모든 엑셀 파일을 열어서 각 파일에 동일한 서식을 적용하는 매크로.
적용할 서식:

A1에 "ABC주식회사" 텍스트 추가 (12pt, 굵게)
A2에 파일명을 제목으로 (14pt, 굵게, 가운데 정렬)
3행 (헤더): 진한 파란 배경 + 흰 글씨 + 굵게 + 가운데 정렬
데이터 영역: 얇은 테두리 적용
열 너비 자동 맞춤
인쇄 설정: A4 가로, 모든 열이 한 페이지에

동작 순서:

폴더 선택 다이얼로그
"X개 파일에 서식을 적용합니다. 계속?" 확인
각 파일 열기 → 서식 적용 → 저장 → 닫기
진행 상황 표시
완료 메시지: "X개 파일 서식 적용 완료"
```

### 실행 후 확인

- 모든 파일에 동일한 서식이 적용되었다
- A1에 회사명, A2에 파일명이 입력되어 있다
- 헤더 행에 파란 배경이 적용되었다
- 인쇄 미리보기가 깔끔하다
- 완료 메시지에 처리 건수가 표시된다

### 주의: 원본 파일이 직접 수정됩니다

이 매크로는 파일을 열고 → 수정하고 → 저장합니다.

한 번 실행하면 되돌릴 수 없습니다. 백업이 필수입니다.

클코에게 백업 기능을 추가로 요청할 수 있습니다:

```
위 코드에 백업 기능을 추가해줘.
서식 적용 전에 각 파일을 "backup" 하위 폴더에 복사한 후 작업하도록.
backup 폴더가 없으면 자동 생성.
```

## 실습 3: 시트를 별도 파일로 분리 저장

### 이런 상황에 씁니다

- 하나의 파일에 부서별 시트가 있는데, 각 부서에 해당 시트만 보내야 한다
- 합쳐진 파일에서 시트별로 파일을 나눠야 한다
- 시트가 20개인 파일을 20개 파일로 만들어야 한다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.

현재 파일의 각 시트를 별도의 엑셀 파일로 저장하는 매크로.
동작:

저장할 폴더 선택 다이얼로그 (기본값: 원본 파일과 같은 폴더)
각 시트를 새 통합문서로 복사
파일명은 시트 이름으로 (예: "영업팀.xlsx")
서식, 수식, 열 너비 등 모두 유지
원본 파일은 건드리지 않음

추가 기능:

숨겨진 시트는 제외 (처리할지 물어보기)
같은 이름의 파일이 이미 있으면 덮어쓸지 물어보기
진행 상황 표시
완료 메시지: "X개 시트를 파일로 저장 완료. 위치: [폴더 경로]"
```

### 실행 후 확인

- 각 시트에 해당하는 파일이 생성되었다
- 파일명이 시트 이름과 같다
- 각 파일의 데이터가 원본 시트와 동일하다
- 서식이 유지되었다
- 원본 파일이 변경되지 않았다

### 응용: 반대로 합치기

```
위와 반대로, 폴더 안의 여러 엑셀 파일을 하나의 파일에
각각 별도 시트로 합치는 매크로도 만들어줘.
```

## 실습 4: 폴더 안 파일 목록 만들기

### 이런 상황에 씁니다

- 폴더에 파일이 몇 개인지, 어떤 파일이 있는지 정리해야 한다
- 파일 크기, 수정 날짜를 한눈에 보고 싶다
- 프로젝트 파일 목록을 문서화해야 한다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.

지정한 폴더의 모든 파일 정보를 엑셀 시트에 정리하는 매크로.
표시할 정보:

A열: 파일명 (확장자 포함)
B열: 확장자
C열: 파일 크기 (KB 또는 MB로 읽기 쉽게)
D열: 마지막 수정 날짜 (2026-03-09 14:30 형식)
E열: 폴더 경로 (하위 폴더인 경우 상대 경로)

옵션:

하위 폴더도 포함 (재귀 탐색)
폴더 선택 다이얼로그 표시
1행 헤더: 파일명, 확장자, 크기, 수정일, 경로
테이블 서식 적용 (헤더 파란색, 테두리)
확장자별로 색 구분 (.xlsx: 초록, .pdf: 빨강, .docx: 파랑)
마지막에 요약: "총 X개 파일, 총 용량 XXX MB"
```

### 실행 후 확인

- "파일목록" 시트가 생성되었다
- 폴더 안의 모든 파일이 나열되어 있다
- 하위 폴더의 파일도 포함되어 있다
- 파일 크기와 수정 날짜가 표시된다
- 확장자별로 색이 다르다
- 하단에 총 파일 수와 총 용량이 표시된다

### 응용: 특정 파일만 검색

```
위 코드를 수정해줘.
엑셀 파일(.xlsx, .xls, .xlsm)만 표시하도록.
```

```
파일명에 특정 키워드가 포함된 것만 표시하도록 수정해줘.
매크로 실행 시 InputBox로 키워드를 입력받아.
```

## 종합 활용: 폴더 관리 도구 만들기

위 4가지 매크로를 하나의 파일에 모아두면 폴더 관리 도구가 됩니다.

```
엑셀 VBA 코드 만들어줘.
아래 4개 매크로를 하나의 모듈에 만들고,
"도구" 시트에 4개 버튼을 만들어줘:

[파일 합치기] 버튼 → MergeFiles 실행
[서식 일괄 적용] 버튼 → ApplyFormatToAll 실행
[시트 분리 저장] 버튼 → SplitSheetsToFiles 실행
[파일 목록] 버튼 → ListFiles 실행
```

이 파일을 `파일관리도구.xlsm`으로 저장해두면, 앞으로 파일 관련 작업이 있을 때 이 파일만 열면 됩니다.

## 주의사항 총정리

### 여러 파일 처리 시 체크리스트

- 백업을 했다 — 처리 대상 폴더를 통째로 복사
- 파일을 닫았다 — 처리 대상 파일이 하나도 열려있지 않다
- 테스트했다 — 연습 파일로 먼저 확인했다
- 경로를 확인했다 — 올바른 폴더를 선택했다

### 자주 발생하는 에러

| 에러 | 원인 | 해결 |
|------|------|------|
| "파일이 이미 열려있습니다" | 처리할 파일이 다른 곳에서 열림 | 모든 파일 닫고 재실행 |
| "경로를 찾을 수 없습니다" | 폴더 경로가 잘못됨 | 폴더 선택 다이얼로그 사용 |
| "사용 권한이 없습니다" | 파일이 읽기 전용 | 파일 속성에서 읽기 전용 해제 |
| 처리 중 엑셀이 멈춤 | 파일이 너무 많거나 큼 | 파일 수를 나눠서 처리 |
| 파일 열기/저장 시 이상한 에러 | 한글 사용자명 경로 문제 | 아래 참조 |

### 한글 사용자명 경로 문제

Windows 사용자명이 한글(예: `C:\Users\홍길동\`)인 경우,

VBA에서 파일 경로를 처리할 때 에러가 발생할 수 있습니다.

해결법:

- 처리할 파일들을 한글이 없는 경로에 두세요
  - 좋은 예: `C:\data\`, `D:\work\`
  - 나쁜 예: `C:\Users\홍길동\바탕 화면\`
- 바탕화면 대신 `C:\claude-projects\` 같은 별도 폴더를 사용하세요

클코에게 코드를 요청할 때 이렇게 말하세요:

```
파일 경로에 한글이 포함될 수 있으니 유니코드 처리해줘.
```

### 성능 팁

파일이 많을 때 매크로가 느릴 수 있습니다. 클코에게 요청하세요:

```
위 코드에 속도 최적화를 추가해줘.
화면 업데이트 끄기, 자동 계산 끄기, 이벤트 끄기.
작업 완료 후 원래대로 복원.
```

이렇게 하면 처리 속도가 3~5배 빨라집니다.

## 직접 해보기: 내 업무에 적용

```
엑셀 VBA 코드 만들어줘.
매달 받는 [XX] 파일들이 폴더에 있는데,
이것들을 합쳐서 [XX] 보고서를 만들어야 해.

구체적으로:

파일 위치: [폴더 경로 또는 "폴더 선택 다이얼로그"]
파일 형식: [.xlsx / .xls / .csv 등]
각 파일 구조: [열 설명]
원하는 결과: [구체적인 결과 설명]
```

## 이번 챕터 핵심 정리

| 매크로 | 용도 | Sub 이름 |
|--------|------|----------|
| 파일 합치기 | 폴더 내 파일 데이터 통합 | MergeFiles |
| 서식 일괄 적용 | 여러 파일에 동일 서식 | ApplyFormatToAll |
| 시트 분리 저장 | 시트별 별도 파일 생성 | SplitSheetsToFiles |
| 파일 목록 | 폴더 내 파일 정보 정리 | ListFiles |

- 여러 파일의 데이터를 하나로 합칠 수 있다
- 여러 파일에 같은 서식을 일괄 적용할 수 있다
- 시트를 별도 파일로 분리 저장할 수 있다
- 폴더 안 파일 목록을 자동으로 만들 수 있다
- 안전 수칙(백업, 파일 닫기, 테스트)을 이해한다

다음 챕터에서는 VBA를 넘어 클코의 스킬 기능을 배웁니다.

자주 하는 작업을 한 줄 명령어로 만들어서 효율을 극대화하는 방법입니다.
