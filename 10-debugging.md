# 에러가 났다! — 클코와 디버깅하는 법.

> CHAPTER 10 / 27 · PART 2 · 가계부 만들기 · 예상 시간 40분

## 에러는 정상입니다

여기까지 오면서 에러를 한 번도 안 만난 사람은 없을 겁니다.

전문 개발자도 하루에 수십 번 에러를 만납니다. 에러는 실패가 아니라 개발의 일부입니다.

이 챕터에서는 에러를 효율적으로 클코에게 넘기는 방법을 배웁니다.

## 에러의 종류

### 1. 빨간 에러 메시지 (터미널)

개발 서버를 실행했을 때 터미널에 빨간 글씨가 나오는 경우:

```
Error: Cannot find module 'next'
```

```
TypeError: Cannot read properties of undefined
```

대처법: 터미널의 에러 메시지를 스크린샷으로 찍어서 클코에 붙여넣기

```
터미널에 이런 에러가 나와:
[스크린샷]
```

### 2. 브라우저 에러 (화면에 표시)

페이지를 열었는데 화면에 에러가 나오는 경우:

```
Application error: a client-side exception has occurred
```

또는 하얀 빈 화면만 나오는 경우.

대처법: F12 키를 눌러서 브라우저 개발자 도구를 엽니다.

Console 탭에 빨간 글씨가 있으면 그걸 스크린샷 찍어서 전달.

```
브라우저 콘솔에 이런 에러가 나와:
[F12 → Console 탭 스크린샷]
```

### 3. 기능이 안 되는 에러 (에러 메시지 없음)

에러 메시지는 없는데, 원하는 대로 동작하지 않는 경우.

예: 버튼을 눌러도 반응이 없음, 데이터가 저장 안 됨 등.

대처법: 뭘 했고, 뭐가 나왔고, 뭘 원하는지 설명

```
"저장" 버튼을 누르면 데이터가 저장되어야 하는데,
버튼을 눌러도 아무 반응이 없어.
콘솔에도 에러가 안 나와.
[현재 화면 스크린샷]
```

### 4. 디자인 깨짐

화면이 이상하게 보이는 경우:

```
모바일에서 보면 이렇게 깨져:
[모바일 스크린샷]
PC에서는 정상인데 모바일에서만 그래.
```

## 디버깅 공식

에러를 클코에게 전달할 때, 이 공식을 따르면 해결이 빨라집니다:

```
[현재 상황] + [에러 내용] + [원하는 결과]
```

### 나쁜 예

```
에러 나는데 고쳐줘
```

→ 클코가 뭐가 에러인지 모름

### 좋은 예

```
현재 상황: 가계부에서 수입을 입력하고 저장 버튼을 눌렀음
에러 내용: 콘솔에 "RLS policy violation" 에러가 남
원하는 결과: 저장 버튼 누르면 Supabase에 데이터가 저장되어야 함
[콘솔 스크린샷]
```

### 최고의 예

```
[스크린샷만 붙여넣기]
이 에러 고쳐줘
```

사실 스크린샷 하나면 대부분 해결됩니다.

클코는 이미지를 읽고 에러 메시지를 정확히 파악합니다.

## 브라우저 개발자 도구 (F12)

웹 개발에서 가장 중요한 도구입니다. 코딩을 몰라도 이것만 알면 됩니다.

### 여는 법

- 브라우저에서 F12 키
- 또는 Ctrl + Shift + I
- 또는 페이지에서 우클릭 → "검사"

### Console 탭

에러 메시지가 빨간색으로 표시됩니다.

이걸 스크린샷 찍어서 클코에 전달하세요. 그게 전부입니다.

### Network 탭

API 호출이 실패했는지 확인할 수 있습니다.

빨간색 행이 있으면 해당 요청이 실패한 것입니다.

개발자 도구를 마스터할 필요 없습니다.

F12 → Console 탭 → 빨간 글씨 스크린샷 이것만 기억하세요.

## 상황별 대처법

### npm 관련 에러

```
Module not found: Can't resolve 'xxx'
```

→ 패키지가 설치 안 된 것

```
이 모듈 설치해줘
```

### 환경변수 관련 에러

환경변수를 바꿨는데 반영이 안 되면:

```
개발 서버 다시 시작해줘
```

`.env.local`을 수정한 후에는 반드시 서버를 재시작해야 합니다.

### 타입 에러

```
Type 'string' is not assignable to type 'number'
```

→ TypeScript 타입 문제. 클코가 잘 고칩니다.

```
이 타입 에러 고쳐줘
[에러 스크린샷]
```

### 무한 로딩 / 페이지 멈춤

```
localhost:3000이 계속 로딩만 하고 안 열려.
터미널에는 이렇게 나와:
[터미널 스크린샷]
```

### 아무것도 안 되면

```
이 프로젝트 전체적으로 한번 점검해줘.
뭐가 문제인지 분석하고 고쳐줘.
```

## 디버깅 프로 팁

### 팁 1: 한 번에 하나씩

에러가 여러 개 나면, 하나씩 고치세요.

한 번에 다 고치라고 하면 새로운 에러가 생길 수 있습니다.

### 팁 2: 고친 후 바로 확인

```
고쳤으면 npm run dev 실행해서 에러 없는지 확인해줘
```

### 팁 3: 3번 시도해도 안 되면 다른 방법

같은 에러를 3번 이상 고치려고 했는데 계속 실패하면:

```
이 방법 말고 다른 방식으로 해결해줘
```

또는 해당 기능을 처음부터 다시 만드는 게 빠를 수 있습니다:

```
이 기능 지금 코드 지우고 처음부터 다시 만들어줘
```

### 팁 4: 클코에게 상태 물어보기

뭐가 문제인지 모르겠을 때:

```
지금 이 프로젝트 상태를 분석해줘.
어디서 에러가 나고 있고, 뭐가 문제인지 정리해줘.
```

## 흔한 에러 모음

| 에러 | 원인 | 해결법 |
|------|------|--------|
| Module not found | 패키지 미설치 | "이 패키지 설치해줘" |
| NEXT_PUBLIC_ 변수 안 먹힘 | 서버 재시작 필요 | "서버 다시 시작해줘" |
| RLS policy 에러 | Supabase 권한 문제 | "RLS 정책 설정해줘" |
| 하얀 빈 페이지 | JS 에러 | F12 → Console 확인 |
| hydration mismatch | 서버/클라이언트 불일치 | 클코에게 에러 전달 |
| EADDRINUSE | 포트 이미 사용 중 | "3000 포트 쓰고 있는 프로세스 종료해줘" |
| npm ERR! ERESOLVE | 패키지 충돌 | "node_modules 삭제하고 npm install 다시 해줘" |

## 핵심 정리

1. 에러 = 정상. 당황하지 마세요.
2. F12 → Console → 스크린샷. 이것만 기억하세요.
3. 스크린샷이 최고. 말로 설명하지 말고 찍어서 붙이세요.
4. 한 번에 하나씩. 에러가 여러 개면 하나씩 고치세요.
5. 3번 실패하면 방법을 바꾸세요. 처음부터 다시 만드는 것도 방법입니다.

이제 Part 2 (가계부)가 끝났습니다!

다음은 Part 3: 고구마마켓 — 진짜 서비스를 만들어봅니다.
