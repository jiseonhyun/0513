# 스킬 — 반복 작업을 자동화하기.

> CHAPTER 26 / 27 · PART 5 · 심화 · 예상 시간 50분

## 이번 챕터의 목표

클코에게 자주 시키는 작업을 스킬로 만들어서
매번 긴 프롬프트를 치지 않고 짧은 명령어 하나로 실행하는 방법을 배웁니다.

Part 4(VBA)에서 엑셀 매크로로 반복 작업을 자동화했습니다.

스킬은 클코 버전의 매크로라고 생각하면 됩니다.

엑셀에서 버튼 하나로 서식을 적용했듯이, 클코에서 한 줄 명령어로 복잡한 작업을 실행합니다.

## 스킬이란?

클코에게 매번 이렇게 길게 설명하는 대신:

```
프로젝트를 배포해줘.
먼저 npm run build로 빌드가 되는지 확인하고,
에러가 있으면 고쳐줘.
빌드가 성공하면 git add 하고 커밋 메시지 만들어서 커밋해줘.
그다음 git push origin main 해줘.
그리고 Vercel 대시보드 열어서 배포 상태 확인해줘.
```

스킬로 만들어두면 이렇게 한 줄이면 됩니다:

```
/deploy
```

스킬 = 자주 하는 작업을 미리 정리해둔 지시서입니다.

레시피에 비유하면:

- 매번 "당근 깎고, 양파 썰고, 기름 두르고..." 하는 대신
- "카레 레시피대로 만들어줘" 한 마디면 끝

## 스킬의 구조

스킬은 단순한 마크다운 파일입니다.

특별한 프로그래밍 지식이 필요 없습니다.

### 저장 위치

```
~/.claude/skills/스킬이름/skill.md
```

Windows에서 `~`는 보통 `C:\Users\사용자이름`입니다.

즉, 실제 경로는:

```
C:\Users\사용자이름\.claude\skills\deploy\skill.md
```

### skill.md 파일 내용 예시

```markdown
# deploy 스킬

설명
프로젝트를 빌드하고 배포합니다.
실행 순서
```

이게 전부입니다. 클코는 이 파일을 읽고 지시대로 실행합니다.

## 스킬 만들기: 클코에게 시키세요

스킬 파일을 직접 만들 수도 있지만, 클코에게 만들어달라고 하면 됩니다.

### 기본 문법

```
"스킬이름"이라는 스킬을 만들어줘.

이 스킬이 하는 일:
```

클코가 알아서 `~/.claude/skills/스킬이름/skill.md` 파일을 생성합니다.

### 만들 때 주의할 점

- 스킬 이름은 영어로 하세요 (폴더명이므로)
- **하이픈(-)**으로 단어 구분: `weekly-report`, `init-project`
- 구체적으로 작성할수록 결과가 좋습니다
- 이 강의에서 쓸 일은 없지만, 고급 사용자는 skill.md를 직접 편집해서 더 세밀하게 조정할 수 있습니다

## 스킬 사용법

만들어진 스킬은 클코 대화 중에 `/스킬이름`으로 실행합니다:

```
/deploy
```

```
/weekly-report
```

```
/cleanup
```

클코가 skill.md 파일을 읽고 거기 적힌 대로 실행합니다.

### 인자(파라미터) 전달

스킬에 추가 정보를 전달할 수도 있습니다:

```
/weekly-report 3월 첫째주
```

```
/deploy staging
```

skill.md에 "인자를 받으면 이렇게 처리해라"고 적어두면 됩니다.

## 실전 예시 1: 프로젝트 초기화 스킬

### 프롬프트

```
"init-project"라는 스킬을 만들어줘.

새 Next.js 프로젝트를 처음부터 세팅하는 스킬이야.
스킬 실행 시 프로젝트 이름을 인자로 받아.
실행 순서:

npx create-next-app@latest [프로젝트이름] --typescript --tailwind --app
cd [프로젝트이름]
shadcn/ui 초기화: npx shadcn@latest init
기본 컴포넌트 설치: button, card, input, dialog
Supabase 클라이언트 설치: npm install @supabase/supabase-js
.env.local 파일 생성 (Supabase URL, Key 빈 칸으로)
CLAUDE.md 파일 생성 (프로젝트 기본 정보)
git init && git add . && git commit -m "Initial setup"
```

### 사용법

```
/init-project my-app
```

이 한 줄로 프로젝트 세팅이 끝납니다. 수동으로 하면 20~30분 걸리는 작업이요.

## 실전 예시 2: 배포 스킬

### 프롬프트

```
"deploy"라는 스킬을 만들어줘.

프로젝트를 안전하게 배포하는 스킬이야.
실행 순서:

빌드 확인: npm run build

실패하면 에러를 분석하고 수정 시도
수정 후 다시 빌드
3번까지 시도, 그래도 실패하면 중단하고 알려줘

타입 체크: npx tsc --noEmit

타입 에러가 있으면 수정

Git 커밋:

git status로 변경사항 확인
변경된 파일 목록을 보여주고
적절한 커밋 메시지를 생성해서 커밋
변경사항이 없으면 이 단계 건너뛰기

Git Push:

git push origin main

배포 확인:

"배포 완료! Vercel에서 빌드 상태를 확인하세요." 메시지
```

### 사용법

```
/deploy
```

## 실전 예시 3: 주간 보고서 스킬

### 프롬프트

```
"weekly-report"라는 스킬을 만들어줘.

Git 커밋 히스토리를 분석해서 주간 작업 보고서를 만드는 스킬이야.
실행 순서:
```

### 사용법

```
/weekly-report
```

금요일 퇴근 전, 이 한 줄이면 주간 보고가 정리됩니다.

## 실전 예시 4: 코드 정리 스킬

### 프롬프트

```
"cleanup"이라는 스킬을 만들어줘.

코드를 깔끔하게 정리하는 스킬이야.
실행 순서:
```

### 사용법

```
/cleanup
```

## 스킬 관리하기

### 스킬 목록 확인

만들어둔 스킬을 확인하려면:

```
내 스킬 목록을 보여줘.
```

또는 직접 폴더를 확인:

```
~/.claude/skills/ 안에 있는 폴더 목록을 보여줘.
```

### 스킬 수정

기존 스킬의 동작을 바꾸고 싶으면:

```
deploy 스킬을 수정해줘.
배포 전에 테스트 실행 단계를 추가해줘.
npm test를 먼저 실행하고, 실패하면 중단.
```

### 스킬 삭제

```
cleanup 스킬을 삭제해줘.
```

## 나만의 스킬 만들기: 아이디어

여러분의 업무에 맞는 스킬을 생각해보세요:

| 스킬 이름 | 용도 |
|----------|------|
| morning-check | 아침에 프로젝트 상태 확인 (에러 로그, 서버 상태) |
| new-page | 새 페이지 생성 (파일 + 라우트 + 컴포넌트) |
| db-backup | 데이터베이스 백업 실행 |
| fix-types | TypeScript 타입 에러 전체 수정 |
| update-deps | 패키지 업데이트 + 빌드 확인 |
| create-api | 새 API 엔드포인트 생성 (파일 + 타입 + 에러처리) |

### 좋은 스킬의 조건

1. 반복적인 작업이어야 합니다 — 한번만 할 작업은 스킬로 만들 필요 없음
2. 순서가 정해져 있어야 합니다 — 매번 순서가 다른 작업은 적합하지 않음
3. 자주 사용해야 합니다 — 1년에 한번 할 작업은 그냥 프롬프트로
4. 구체적이어야 합니다 — "코드 잘 짜줘"는 스킬이 아닙니다

### 스킬 만들기 프롬프트 템플릿

```
"[스킬이름]"이라는 스킬을 만들어줘.

[한 줄 설명].
실행 순서:

[첫 번째 단계]
[두 번째 단계]
[세 번째 단계]

주의사항:

[지켜야 할 규칙]
[에러 처리 방법]

결과 보고:
```

## 이번 챕터 핵심 정리

- 스킬 = 자주 하는 작업을 한 줄 명령어로 만든 것
- 스킬 파일 위치: `~/.claude/skills/스킬이름/skill.md`
- 만들기: "XX라는 스킬을 만들어줘"
- 사용하기: `/스킬이름`
- 수정하기: "XX 스킬을 수정해줘"
- 나만의 스킬 하나 이상 만들어봤다

다음 챕터에서는 스킬보다 한 단계 진화한 에이전트를 배웁니다.

스킬이 "레시피"라면 에이전트는 "요리사"입니다.

상황을 판단하고 스스로 결정하는 AI 작업자를 만들어봅니다.
