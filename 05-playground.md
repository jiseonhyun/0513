# 놀아보기 — 테트리스, 갤러그, 슈퍼마리오.

> CHAPTER 05 / 27 · PART 1 · 시작하기 · 예상 시간 40분

---

## 이번 챕터의 목표

클로드코드가 진짜 되는지 직접 체험해봅시다.  
코드를 이해할 필요 없습니다. 시키고, 구경하고, 플레이하세요.

지금까지 배운 프롬프트 기술을 써먹어 보는 시간입니다.  
테트리스 하나만 만들어봐도 클코의 능력을 충분히 체험할 수 있습니다.  
나머지(갤러그, 슈퍼마리오)도 도전해보세요!

---

## 준비

새 프로젝트 폴더를 만들고 클로드코드를 실행합니다:

```
cd C:\claude-projects
mkdir playground
cd playground
claude
```

---

## 테트리스 만들기

아래 프롬프트를 그대로 복사해서 클코에 붙여넣으세요:

```
테트리스 게임을 만들어줘.

기능:
- 방향키 좌우로 블록 이동
- 위 방향키로 블록 회전
- 아래 방향키로 빠르게 내리기
- 스페이스바로 한번에 바닥까지 내리기 (하드드롭)
- 한 줄이 가득 차면 줄 삭제 + 점수 획득
- 다음에 나올 블록 미리보기
- 점수와 레벨 표시
- 레벨 올라갈수록 속도 증가
- 게임오버 시 최종 점수 표시 + 재시작 버튼
```

### 클코가 하는 일 관찰하기

클코가 작업하는 동안 이런 것들을 관찰해보세요:

1. **파일 생성** — `index.html` 같은 파일을 만듭니다
2. **코드 작성** — 게임 로직을 짭니다 (이해 안 해도 됩니다)
3. **브라우저 실행** — 완성 후 브라우저에서 열어줍니다

### 만들어진 후

브라우저에 테트리스가 나타납니다. 직접 플레이해보세요!

마음에 안 드는 부분이 있으면 수정 요청:

```
블록 색깔을 더 화려하게 바꿔줘. 네온 느낌으로.
```
```
배경에 격자 무늬 추가해줘
```
```
게임오버 때 폭발 효과 넣어줘
```

---

## 갤러그 만들기

새 대화를 시작합니다:

```
/clear
```

```
갤러그 스타일 슈팅 게임을 만들어줘.

기능:
- 내 우주선: 좌우 방향키로 이동
- 스페이스바로 총알 발사
- 적 우주선이 위에서 대열을 지어 내려옴
- 적이 좌우로 움직이다가 아래로 내려옴
- 적도 가끔 총알을 쏨
- 적 맞추면 점수 획득 (적 종류별 점수 다르게)
- 목숨 3개, 다 잃으면 게임오버
- 스테이지 클리어 시 다음 스테이지 (더 빠르고 많은 적)
```

### 수정 예시
```
내 우주선 디자인을 더 멋있게 바꿔줘
```
```
보스 적 추가해줘. 3스테이지마다 나오고, 체력이 높은 큰 적.
```

---

## 슈퍼마리오 만들기

```
/clear
```

```
슈퍼마리오 스타일 2D 플랫포머 게임을 만들어줘.

기능:
- 캐릭터: 좌우 방향키로 이동, 위 방향키 또는 스페이스바로 점프
- 물리: 중력 적용, 자연스러운 점프 곡선
- 발판: 여러 높이의 플랫폼, 밟고 올라갈 수 있음
- 코인: 먹으면 점수 획득, 반짝이는 애니메이션
- 적: 좌우로 왔다갔다하는 적, 위에서 밟으면 처치, 옆에서 부딪히면 사망
- 골인 지점: 깃발에 도착하면 스테이지 클리어
- 3개 이상의 스테이지
- 목숨 시스템
```

---

## 자유 실습 — 뭐든 시켜보세요

여기서부터는 아무거나 시켜보는 시간입니다.  
아래는 예시일 뿐, 만들고 싶은 걸 자유롭게 시도하세요.

### 게임

```
지뢰찾기 만들어줘.
- 초급(9x9, 지뢰 10개), 중급(16x16, 40개), 고급(30x16, 99개)
- 우클릭으로 깃발, 좌클릭으로 열기
- 타이머와 남은 지뢰 수 표시
```
```
2048 게임 만들어줘.
- 방향키로 조작
- 타일 합쳐질 때 애니메이션
- 최고 점수 저장 (localStorage)
```
```
뱀 게임(Snake) 만들어줘.
- 방향키로 방향 전환
- 사과 먹으면 길어짐
- 벽이나 자기 몸에 부딪히면 게임오버
```

### 도구

```
그림판 만들어줘.
- 색깔 선택 (팔레트)
- 브러시 크기 조절
- 지우개
- 전체 지우기
- 그림 다운로드 (PNG)
```
```
타자 연습 프로그램 만들어줘. 한글로.
- 문장이 나오면 따라 치기
- 정확도와 타수(WPM) 표시
- 틀린 글자 빨간색 표시
```
```
포모도로 타이머 만들어줘.
- 25분 집중 → 5분 휴식 → 반복
- 알림 소리
- 오늘 몇 세트 했는지 표시
```

---

## 수정 실험해보기

게임이 만들어지면, 앞 챕터에서 배운 프롬프트 기술을 활용해보세요:

```
전체적으로 다크 모드로 바꿔줘. 네온 느낌.
```
```
최고 점수를 저장하고 보여주는 기능 추가해줘
```
```
너무 어려워. 적 속도를 절반으로 줄여줘
```
```
이 게임은 좋은데, 이 부분의 디자인을 이렇게 바꿔줘
[참고 이미지 붙여넣기]
```

---

## 이번 챕터에서 확인한 것

- ✅ 코드를 한 줄도 안 짰는데 게임이 만들어졌다
- ✅ 한국어로 말하면 된다
- ✅ 마음에 안 들면 수정 요청하면 된다
- ✅ 구체적으로 요청할수록 결과가 좋다
- ✅ 이미지를 붙여넣으면 더 정확하다

다음 챕터부터는 이 능력으로 진짜 쓸모 있는 것을 만들어봅니다.  
첫 번째 프로젝트: **가계부 웹앱.**
