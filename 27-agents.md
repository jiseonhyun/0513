# 에이전트 — AI에게 일 맡기기.

> CHAPTER 27 / 27 · PART 5 · 심화 · 예상 시간 40분

## 이번 챕터의 목표

스킬보다 한 단계 진화한 에이전트 개념을 이해합니다.
클코가 다른 AI를 불러서 일을 시키는 서브에이전트를 배웁니다.
그리고 이 강의 전체를 마무리합니다.

## 스킬 vs 에이전트

지난 챕터에서 배운 스킬과 이번에 배울 에이전트의 차이를 이해해봅시다.

### 스킬 = 레시피

스킬은 정해진 순서대로 실행합니다.

```
1단계: 빌드 → 2단계: 테스트 → 3단계: 배포
```

레시피처럼 순서가 정해져 있습니다.
"만약 이러면 저렇게" 같은 판단은 제한적입니다.

### 에이전트 = 요리사

에이전트는 상황을 판단하고 스스로 결정합니다.

```
"이 재료로 저녁을 만들어줘"
→ 재료를 보고 → 메뉴를 정하고 → 조리법을 선택하고 → 완성
```

같은 요청이라도 상황에 따라 다르게 행동합니다.

### 비교표

| | 스킬 | 에이전트 |
|---|---|---|
| 비유 | 레시피 | 요리사 |
| 실행 방식 | 정해진 순서대로 | 상황 판단 후 결정 |
| 유연성 | 낮음 (순서 고정) | 높음 (상황 적응) |
| 적합한 작업 | 반복 작업 | 복잡한 작업 |
| 예시 | "빌드→커밋→푸시" | "코드를 리뷰하고 개선해줘" |

## 에이전트의 실제 동작

에이전트가 어떻게 일하는지 구체적으로 살펴봅시다.

### 예시: "이 페이지에 검색 기능을 추가해줘"

스킬이라면 정해진 순서만 따릅니다.

에이전트는 이렇게 스스로 판단하며 일합니다:

```
1. 현재 코드 구조를 파악한다
   → "Next.js App Router를 쓰고 있구나"
   → "Supabase를 DB로 쓰고 있구나"
```

사실 여러분이 지금까지 클코에게 뭔가를 시킬 때마다, 클코는 이미 에이전트처럼 일하고 있었습니다.

## 서브에이전트: AI가 AI를 불러서 일시키기

클코의 강력한 기능 중 하나는 서브에이전트입니다.

클코가 다른 AI 작업자를 불러서 일을 나눠 시키는 것입니다.

### 어떻게 동작하나

```
나 (사용자): "프로젝트 전체를 점검해줘"

클코 (메인 에이전트):
  → 서브에이전트 1: "코드 품질을 검사해"
  → 서브에이전트 2: "보안 취약점을 찾아"
  → 서브에이전트 3: "성능 문제를 분석해"
```

각 서브에이전트는 독립적으로 동시에 일합니다.

혼자 하면 30분 걸릴 작업이 10분에 끝날 수 있습니다.

### 서브에이전트 프롬프트 예시

```
이 프로젝트를 점검해줘. 서브에이전트로 병렬 처리해.

에이전트 1 - 코드 리뷰:

사용하지 않는 코드 찾기
중복 코드 찾기
개선 포인트 제안

에이전트 2 - 타입 안전성:

TypeScript 타입 에러 찾기
any 타입 사용 찾기
타입 개선 제안

에이전트 3 - 성능:

불필요한 리렌더링 찾기
큰 번들 사이즈 원인 찾기
이미지 최적화 확인
```

### 서브에이전트를 쓰면 좋은 상황

- 여러 파일을 동시에 분석해야 할 때
- 독립적인 작업을 병렬로 처리할 때
- 대규모 코드베이스를 검토할 때

## CLAUDE.md 복습: 에이전트에게 규칙 정해주기

12장에서 배운 CLAUDE.md를 에이전트 관점에서 다시 봅시다.

CLAUDE.md는 단순한 설명서가 아니라, 에이전트의 행동 규칙입니다.

에이전트가 상황을 판단할 때 CLAUDE.md에 적힌 규칙을 자동으로 따릅니다.

12장에서 기본적인 CLAUDE.md를 만들었다면, 이제 에이전트가 더 똑똑하게 일하도록 규칙을 강화할 수 있습니다:

```
CLAUDE.md에 아래 규칙을 추가해줘:

코딩 규칙:

새 파일 생성 시 반드시 TypeScript 사용
API 응답은 항상 try-catch로 에러 처리
console.log 남기지 말 것

작업 방식:
```

CLAUDE.md의 기본 사용법은 12장에서 자세히 다루었습니다.

기억이 안 나면 12장을 다시 확인해보세요.

## 에이전트 활용 실전 예시

### 코드 리뷰 에이전트

```
내가 수정한 코드를 리뷰해줘.

확인할 것:

버그 가능성이 있는 코드
보안 취약점 (SQL 인젝션, XSS 등)
성능 문제
코딩 컨벤션 위반
더 좋은 방법이 있는지
```

### 에러 디버깅 에이전트

```
이 에러를 해결해줘:
[에러 메시지 붙여넣기]

분석 순서:
```

### 기능 구현 에이전트

```
로그인 기능을 구현해줘.

요구사항:

카카오 OAuth 로그인
로그인 후 /mypage로 리다이렉트
사용자 정보를 DB에 저장
로그인 상태 유지 (세션)
로그아웃 기능
```

## 이 강의에서 배운 것들

전체 여정을 정리해봅시다.

### Part 1: 시작하기 (01~05)

| 챕터 | 배운 것 |
|------|---------|
| 01. 오프닝 | 클코 소개, 강의 로드맵 |
| 02. 환경 설치 | Node.js, VS Code, Git, 클코 설치 |
| 03. 기본 사용법 | 클코 실행, 대화 흐름, 슬래시 명령어 |
| 04. 프롬프트법 | 구체적으로 말하기, 한 번에 하나씩 |
| 05. 놀아보기 | 게임 만들기로 체험, 수정 실험 |

### Part 2: 가계부 만들기 (06~10)

| 챕터 | 배운 것 |
|------|---------|
| 06. 가계부 시연 | 시연 관찰, 프롬프트 패턴 |
| 07. 가계부 실습 | 직접 만들기, 수정 요청 연습 |
| 08. MCP & Supabase | MCP 개념, Supabase DB 연결 |
| 09. DB 붙이기 | 데이터 영구 저장, 환경변수 |
| 10. 디버깅 | 에러 종류별 대처법, F12 콘솔 |

### Part 3: 고구마마켓 (11~20)

| 챕터 | 배운 것 |
|------|---------|
| 11. Git/GitHub | 코드 버전관리, 클라우드 백업 |
| 12. CLAUDE.md | 프로젝트 설명서, 일관성 유지 |
| 13. 상품 목록 | 프로젝트 생성, DB 테이블, 메인 페이지 |
| 14. CRUD | 등록/상세/수정/삭제 전체 기능 |
| 15. 소셜 로그인 | 카카오/구글 OAuth, 권한 관리 |
| 16. Context7 MCP | 최신 기술 문서 실시간 참조 |
| 17. 토스 결제 | 결제 위젯, confirm API, 테스트 모드 |
| 18. Playwright MCP | 브라우저 자동 조작, 테스트 자동화 |
| 19. 코드 리뷰 | 5가지 검증 방법, 보안 점검 |
| 20. Vercel 배포 | 인터넷 공개, 자동 배포, 환경변수 |

### Part 4: VBA 업무 자동화 (21~25)

| 챕터 | 배운 것 |
|------|---------|
| 21. VBA 소개 | 엑셀 자동화 개념, 워크플로우 |
| 22. 첫 매크로 | 날짜 입력, 서식, 버튼 만들기 |
| 23. 데이터 정리 | 중복 제거, 분류, 형식 통일 |
| 24. 보고서 | 요약, 차트, PDF, 파이프라인 |
| 25. 다중 파일 | 파일 합치기, 일괄 처리 |

### Part 5: 심화 (26~27)

| 챕터 | 배운 것 |
|------|---------|
| 26. 스킬 | 반복 작업을 한 줄 명령어로 |
| 27. 에이전트 | 서브에이전트, AI에게 일 맡기기 |

## 앞으로의 여정

이 강의를 마치면 여러분은:

- 웹사이트를 만들 수 있습니다 (가계부, 쇼핑몰)
- 엑셀 업무를 자동화할 수 있습니다 (VBA 매크로)
- 반복 작업을 스킬로 만들 수 있습니다
- 복잡한 작업을 에이전트에게 맡길 수 있습니다

### 더 배우고 싶다면

이 강의에서 배운 기본기로 더 많은 것을 만들 수 있습니다:

- 블로그 만들기
- 포트폴리오 사이트 만들기
- 사내 관리 도구 만들기
- 챗봇 만들기
- 데이터 대시보드 만들기

방법은 항상 같습니다:

"뭘 만들고 싶은지 클코에게 설명하기."

### 핵심 습관

- 01. 구체적으로 설명하기 — "좋은 사이트 만들어줘" 대신 "예산 관리 앱을 만들어줘. 수입/지출 입력, 월별 차트..."
- 02. 에러를 두려워하지 않기 — 에러 메시지를 클코에게 그대로 보여주면 됩니다
- 03. 작게 시작하기 — 한 번에 완벽한 걸 만들려 하지 말고, 작은 기능부터
- 04. 자주 확인하기 — 한 기능 만들 때마다 실행해서 확인

## 마무리

```
코드를 몰라도, AI와 함께 만들 수 있는 시대입니다.
중요한 것은 "뭘 만들고 싶은지" 아는 것입니다.
```

여러분은 이미 그 "뭘 만들고 싶은지"를 가지고 있습니다.

매일 반복하는 엑셀 작업, 만들어보고 싶었던 앱, 자동화하고 싶었던 업무.

이제 그걸 실현할 도구를 가졌습니다.

클코에게 말을 거세요. 나머지는 AI가 합니다.

## 최종 체크리스트

이 강의를 통해 할 수 있게 된 것들:

### 기본

- 클코를 설치하고 실행할 수 있다
- 클코에게 효과적으로 프롬프트를 작성할 수 있다
- CLAUDE.md의 역할을 이해한다

### 웹 개발

- 간단한 웹 앱을 클코와 함께 만들 수 있다
- 데이터베이스와 연결하는 앱을 만들 수 있다
- 로그인, 결제 등 실전 기능을 구현할 수 있다

### 엑셀 자동화

- VBA 에디터를 열고 매크로를 실행할 수 있다
- 데이터 정리 매크로를 만들 수 있다
- 보고서 자동 생성 매크로를 만들 수 있다
- 여러 파일을 한번에 처리하는 매크로를 만들 수 있다

### 심화

- 스킬을 만들어서 반복 작업을 자동화할 수 있다
- 에이전트의 개념을 이해하고 활용할 수 있다
- 에러가 나도 당황하지 않고 해결할 수 있다

모든 체크박스에 체크할 수 있다면, 여러분은 AI 시대의 메이커입니다.

수고하셨습니다!
