# 보고서 자동 생성.

> CHAPTER 24 / 27 · PART 4 · VBA 업무 자동화 · 예상 시간 40분

## 이번 챕터의 목표

매번 수동으로 만드는 보고서를 버튼 하나로 자동 생성합니다.

요약 테이블, 차트, 월별 양식, PDF 내보내기까지

마지막에는 이 모든 걸 한번에 실행하는 파이프라인을 만듭니다.

매달 마감일 앞에서 보고서 만드느라 야근한 경험 있으신가요?

데이터 복사 → 합계 계산 → 차트 그리기 → 양식 맞추기 → PDF 변환 → 이메일 발송...

이 모든 과정을 버튼 하나로 끝내는 것이 이번 챕터의 목표입니다.

이전 챕터(22~23장)에서 배운 개별 매크로들을 조합하는 방법을 배웁니다.

## 준비: 연습용 매출 데이터

보고서를 만들려면 데이터가 필요합니다.

실제 업무 데이터가 없다면 연습용 데이터를 만들어주세요:

```
엑셀 VBA 코드 만들어줘.
"데이터"라는 시트를 만들고 아래 구조로 매출 샘플 데이터 200행을 넣어줘:

A열: 날짜 (2026년 1월 1일 ~ 3월 31일 사이 랜덤)
B열: 부서 (영업1팀, 영업2팀, 마케팅팀, 기술지원팀 중 랜덤)
C열: 항목 (제품판매, 서비스, 컨설팅, 유지보수 중 랜덤)
D열: 금액 (50만 ~ 5000만 사이 랜덤, 만원 단위)
E열: 담당자 (김영희, 이철수, 박민지, 정우성, 한지민 중 랜덤)
```

## 실습 1: 요약 보고서 생성

### 이런 상황에 씁니다

- 원본 데이터가 수백~수천 행인데 요약 보고서를 만들어야 한다
- 부서별 합계, 항목별 합계를 표로 정리해야 한다
- 매번 피벗 테이블을 만들기 번거롭다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
"데이터" 시트에 아래 컬럼이 있어:
- A열: 날짜
- B열: 부서
- C열: 항목
- D열: 금액

이 데이터를 분석해서 "요약보고서"라는 새 시트에 보고서를 만들어줘:
[보고서 구성]

상단: 보고서 제목 "월간 매출 요약 보고서", 작성일자
부서별 합계 테이블:
부서명 | 건수 | 합계 | 평균 | 비율(%)
합계 행 포함

항목별 합계 테이블:
항목명 | 건수 | 합계 | 평균 | 비율(%)
합계 행 포함

월별 합계 테이블:
월 | 건수 | 합계

최하단: 전체 합계, 전체 건수, 전체 평균

서식:

제목: 16pt 굵게
테이블 헤더: 진한 파란 배경 + 흰 글씨
금액: ₩1,000,000 형식
비율: 소수점 1자리 (예: 35.2%)
테이블 간 빈 행 2줄
모든 테이블에 테두리 적용
```

### 실행 후 확인

- "요약보고서" 시트가 생성되었다
- 부서별 합계가 정확하다 (원본 데이터와 수동 대조)
- 항목별 합계가 정확하다
- 비율(%)의 합이 100%이다
- 서식이 깔끔하게 적용되었다

### 응용: 특정 기간만 요약

```
위 코드를 수정해줘.
매크로 실행 시 시작 날짜와 종료 날짜를 물어보고,
해당 기간의 데이터만 요약하도록.
```

## 실습 2: 차트 자동 생성

### 이런 상황에 씁니다

- 보고서에 차트를 넣어야 하는데 매번 만들기 번거롭다
- 데이터가 바뀔 때마다 차트도 다시 만들어야 한다
- 여러 종류의 차트를 한번에 만들고 싶다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
"요약보고서" 시트의 데이터로 차트를 만드는 매크로.

만들 차트:

부서별 매출 막대 차트

가로: 부서명
세로: 합계 금액
차트 제목: "부서별 매출 현황"
각 막대에 금액 레이블 표시
색상: 파란 계열

항목별 매출 원형 차트

각 항목의 비율 표시
차트 제목: "항목별 매출 비율"
각 조각에 항목명 + 비율(%) 표시

월별 추이 꺾은선 차트

가로: 월
세로: 합계 금액
차트 제목: "월별 매출 추이"
마커 표시

차트 위치:

"차트" 라는 새 시트에 3개 차트를 세로로 배치
각 차트 크기: 가로 600px, 세로 350px
차트 간 간격 적당히
```

### 실행 후 확인

- "차트" 시트가 생성되었다
- 막대 차트에 부서별 금액이 표시된다
- 원형 차트에 항목별 비율이 표시된다
- 꺾은선 차트에 월별 추이가 표시된다
- 차트 제목, 레이블이 보기 좋게 표시된다

### 차트 스타일 변경

```
막대 차트를 가로 막대(수평)로 바꿔줘.
그리고 금액이 큰 순서대로 정렬해줘.
```

```
원형 차트를 도넛 차트로 바꿔줘. 가운데에 전체 합계 금액 표시.
```

## 실습 3: 월별 보고서 양식 생성

### 이런 상황에 씁니다

- 매달 같은 양식의 보고서를 만들어야 한다
- 회사 로고, 제목, 양식이 정해져 있다
- 해당 월의 데이터만 자동으로 채워지면 좋겠다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
월별 보고서 양식을 자동 생성하는 매크로.

매크로 실행 시:

InputBox로 보고할 월 입력 받기 (예: "3")
"보고서_2026년_3월" 형식으로 새 시트 생성

시트 구성:
[1행~3행: 헤더 영역]

A1: "ABC 주식회사" (14pt, 굵게)
A2: "2026년 3월 월간 매출 보고서" (18pt, 굵게, 가운데 정렬)
A3: "작성일: 2026-03-09 | 작성자: ___" (10pt, 오른쪽 정렬)
A1:F3 병합 및 가운데 정렬

[5행~: 데이터 영역]

"데이터" 시트에서 해당 월의 데이터만 가져오기
헤더: 날짜 | 부서 | 항목 | 금액 | 담당자
테이블 서식 적용

[데이터 아래 3행 비우고: 요약 영역]

전체 합계: ₩XX,XXX,XXX
전체 건수: XX건
평균 금액: ₩XX,XXX,XXX

서식:

인쇄 영역 자동 설정
A4 용지 가로 방향
여백: 보통
헤더/푸터: 페이지 번호
```

### 실행 후 확인

- 입력한 월에 해당하는 시트가 생성되었다
- 시트 이름이 "보고서_2026년_3월" 형식이다
- 해당 월의 데이터만 들어있다
- 합계, 건수, 평균이 정확하다
- 인쇄 미리보기에서 양식이 깔끔하다

### 응용: 여러 달 한번에

```
위 코드를 수정해줘.
1월부터 3월까지 한번에 3개 시트를 만들도록.
```

## 실습 4: PDF로 내보내기

### 이런 상황에 씁니다

- 보고서를 PDF로 변환해서 이메일로 보내야 한다
- 매번 "다른 이름으로 저장 → PDF" 하기가 번거롭다
- 여러 시트를 각각 PDF로 저장하고 싶다

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
현재 활성 시트를 PDF로 저장하는 매크로.

설정:

파일명: "[시트이름]_2026-03-09.pdf" (오늘 날짜 자동)
저장 위치: 원본 엑셀 파일과 같은 폴더
인쇄 품질: 고품질
용지: A4, 가로 방향
모든 열이 한 페이지에 들어가도록 자동 조정

저장 후:

"PDF 저장 완료!" 메시지 + 저장 경로 표시
PDF 파일을 자동으로 열기 (기본 PDF 뷰어로)
```

### 실행 후 확인

- PDF 파일이 지정 위치에 저장되었다
- 파일명에 시트 이름과 오늘 날짜가 포함되어 있다
- PDF를 열면 내용이 깔끔하게 나온다
- 모든 열이 한 페이지 안에 들어간다

### 응용: 여러 시트를 각각 PDF로

```
현재 통합 문서의 모든 시트를 각각 별도의 PDF 파일로 저장해줘.
파일명: "시트이름_날짜.pdf"
저장 위치는 폴더 선택 다이얼로그로.
```

### 응용: 하나의 PDF로 합치기

```
"요약보고서"와 "차트" 시트를 하나의 PDF 파일로 합쳐서 저장해줘.
```

## 조합하기: 원클릭 보고서 파이프라인

직접 만들어보고 싶다면 개별 매크로(실습 1~2)부터 시작하는 것을 추천합니다.

파이프라인 전체를 한번에 만드는 것은 개별 매크로에 익숙해진 후 도전하세요.

이제 위의 4가지를 한번에 실행하는 매크로를 만들어봅시다.

버튼 하나 누르면 데이터 분석부터 PDF 저장까지 자동으로 처리됩니다.

### 클코에게 보내는 프롬프트

```
엑셀 VBA 코드 만들어줘.
"보고서 생성" 매크로를 만들어줘. 아래 순서로 전부 실행:

데이터 확인

"데이터" 시트가 있는지 확인
데이터가 비어있으면 중단 + 에러 메시지

요약 보고서 생성

부서별, 항목별 합계 테이블
"요약보고서" 시트에 생성

차트 생성

부서별 막대차트, 항목별 원형차트, 월별 꺾은선
"차트" 시트에 생성

월별 보고서 생성

이번 달 기준으로 월간 보고서 양식 생성

PDF 저장

"요약보고서" 시트를 PDF로 저장
파일명: "월간보고서_2026-03-09.pdf"
엑셀 파일과 같은 폴더에 저장

완료 처리

진행 상황을 상태표시줄에 표시 (1/5, 2/5, ...)
완료 메시지: "보고서 생성 완료! PDF 위치: [경로]"
요약보고서 시트로 이동
```

### 실행 후 확인

- 상태표시줄에 진행 상황이 표시된다
- "요약보고서" 시트가 생성되었다
- "차트" 시트에 3개 차트가 있다
- 월간 보고서 시트가 생성되었다
- PDF 파일이 저장되었다
- 완료 메시지에 PDF 경로가 표시된다

## 실전 팁: 보고서 매크로를 업무에 맞게 커스터마이징

### 프롬프트 전략

실제 업무에 적용할 때는 클코에게 여러분의 보고서 양식을 설명하세요:

```
엑셀 VBA 코드 만들어줘.
우리 회사 주간 보고서를 자동 생성하는 매크로.

우리 회사 보고서 양식:

상단: 회사 로고 텍스트 "[회사명]", 보고서 제목, 보고 기간
중간: [실제 보고서에 들어가는 내용 설명]
하단: 작성자, 결재란

데이터 시트 구성: [실제 열 구성 설명]
```

### 한번 만들면 계속 쓸 수 있습니다

보고서 매크로는 한번 잘 만들어두면:

- 매주/매달 버튼 하나로 보고서 생성
- 데이터만 업데이트하면 보고서가 자동으로 바뀜
- 일관된 양식과 서식으로 퀄리티 유지
- 보고서 작성 시간: 1시간 → 1분

## 이번 챕터 핵심 정리

| 매크로 | 용도 | Sub 이름 |
|--------|------|----------|
| 요약 보고서 | 부서별/항목별 합계 테이블 | CreateSummaryReport |
| 차트 생성 | 막대/원형/꺾은선 차트 | CreateCharts |
| 월별 보고서 | 양식에 맞는 보고서 | CreateMonthlyReport |
| PDF 내보내기 | 시트를 PDF로 저장 | ExportToPDF |
| 전체 파이프라인 | 위 4개를 한번에 | GenerateFullReport |

- 요약 보고서를 자동으로 생성할 수 있다
- 차트를 자동으로 만들 수 있다
- 월별 보고서 양식을 자동 생성할 수 있다
- PDF로 내보내기를 자동화할 수 있다
- 전체 과정을 원클릭으로 실행하는 파이프라인을 만들 수 있다

다음 챕터에서는 여러 엑셀 파일을 한번에 처리하는 매크로를 만듭니다.

폴더 안의 파일을 합치거나, 같은 작업을 일괄 적용하는 방법을 배웁니다.
