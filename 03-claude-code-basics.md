# 클로드코드 기본 사용법.

> CHAPTER 03 / 27 · PART 1 · 시작하기 · 예상 시간 40분

---

## 클로드코드 실행하기

### 프로젝트 폴더에서 시작하는 게 핵심

클로드코드는 현재 폴더를 기준으로 동작합니다.  
바탕화면에서 실행하면 바탕화면이 프로젝트가 되고,  
프로젝트 폴더에서 실행하면 그 폴더가 프로젝트가 됩니다.

```
cd C:\claude-projects
mkdir my-first-project
cd my-first-project
claude
```

반드시 프로젝트 폴더 안에서 `claude`를 실행하세요.  
클코가 이 폴더의 모든 파일을 읽고 이해합니다.

### 실행하면 보이는 화면

Claude Code 시작 화면:
- ✻ Welcome to Claude Code!
- /help for help
- cwd: C:\claude-projects\my-first-project

`>` 표시가 나타나면 여기에 한국어로 말하면 됩니다.

---

## 대화 흐름 — 실제로 어떻게 진행되나

클로드코드에게 "로그인 페이지 만들어줘"라고 입력하면, 이런 흐름으로 진행됩니다:

### 1단계: 클코가 계획을 세움

```
로그인 페이지를 만들겠습니다.
- src/app/login/page.tsx 파일을 생성합니다
- 이메일/비밀번호 입력 폼을 만듭니다
- ...
```

### 2단계: 파일을 만들거나 수정할 때 허락을 구함

```
Write to src/app/login/page.tsx?
```

여기서 선택지가 3개 있습니다:

| 키 | 의미 | 언제 쓰나 |
|---|---|---|
| `y` | 이번 한 번만 허락 | 뭘 하는지 확인하면서 진행할 때 |
| `n` | 거부 | 원하지 않는 수정일 때 |
| `a` | 이 세션에서 항상 허락 | 클코를 믿고 맡길 때 |

처음에는 `y`로 하나씩 확인하세요.  
익숙해지면 `a`를 눌러서 속도를 높일 수 있습니다.

### 3단계: 터미널 명령어 실행 허락

클코가 npm 설치나 서버 실행 같은 터미널 명령어도 실행합니다.  
이때도 허락을 구합니다:

```
Run command: npm install next react react-dom?
```

### 4단계: 완료

```
로그인 페이지를 만들었습니다.
npm run dev로 확인해보세요.
```

---

## 슬래시 명령어

`/`로 시작하는 명령어는 클코에게 하는 특수 지시입니다.  
대화 도중에 아무 때나 입력할 수 있습니다.

### 필수 명령어

| 명령어 | 설명 | 언제 쓰나 |
|---|---|---|
| `/exit` | 클로드코드 종료 | 작업 끝났을 때 |
| `/clear` | 대화 기록 전부 삭제하고 새로 시작 | 주제를 완전히 바꿀 때 |
| `/compact` | 대화 기록을 요약해서 압축 | 대화가 길어져서 느려질 때 |
| `/help` | 도움말 보기 | 명령어가 기억 안 날 때 |

### 자주 쓰는 명령어

| 명령어 | 설명 |
|---|---|
| `/model` | 사용할 AI 모델 변경 (Opus, Sonnet 등) |
| `/cost` | 현재 세션에서 사용한 비용 확인 |
| `/init` | CLAUDE.md 파일 자동 생성 (챕터 12에서 배움) |

### /compact — 가장 중요한 명령어

클코와 오래 대화하다 보면 대화 내용이 쌓여서 느려집니다.  
이때 `/compact`를 입력하면:

- 지금까지의 대화를 요약합니다
- 메모리를 확보해서 다시 빨라집니다
- 중요한 맥락은 유지됩니다

대화가 20~30번 이상 오갔으면 `/compact`를 한 번 해주세요.

---

## 이미지 붙여넣기 — 클코의 킬러 기능

클로드코드는 이미지를 이해합니다. 이 기능이 정말 강력합니다.

### 에러 화면 스크린샷

에러가 나면 말로 설명하지 마세요. 스크린샷을 찍어서 붙여넣으세요.

1. 에러 화면에서 `Win + Shift + S`로 스크린샷
2. 클로드코드 터미널에서 `Ctrl + V`로 붙여넣기
3. "이 에러 고쳐줘"라고 말하기

클코가 에러 메시지를 읽고 원인을 분석하고 직접 고칩니다.

### 디자인 참고 이미지

마음에 드는 웹사이트 디자인을 스크린샷 찍어서:

```
이런 느낌으로 만들어줘
[Ctrl+V로 이미지 붙여넣기]
```

### 브라우저 화면 공유

현재 만든 웹사이트의 문제를 보여줄 때:

```
지금 이렇게 보이는데, 여기서 버튼이 안 눌려
[화면 스크린샷 붙여넣기]
```

스크린샷 한 장이 설명 열 줄보다 낫습니다.

---

## 되돌리기

클코가 뭔가 잘못 만들었을 때, 되돌리는 방법이 여러 가지 있습니다.

### 방법 1: 클코에게 시키기

```
방금 한 거 되돌려줘
```

클코가 자기가 방금 수정한 파일들을 원래대로 되돌립니다.

### 방법 2: 특정 부분만 되돌리기

```
방금 로그인 페이지에 추가한 거 빼줘. 나머지는 유지하고.
```

### 방법 3: Ctrl+C로 중단

클코가 뭔가 하고 있는 도중에 멈추고 싶으면 `Ctrl+C`를 누르세요.  
진행 중이던 작업이 중단됩니다.

---

## 클로드코드의 한계

| 할 수 있는 것 | 못하는 것 |
|---|---|
| 코드 작성, 수정, 삭제 | 화면을 직접 보면서 작업 (눈이 없음) |
| 터미널 명령어 실행 | 브라우저를 직접 조작 (MCP로 가능, 챕터 18) |
| 파일 읽기 | 인터넷 검색 (MCP로 가능) |
| 에러 분석 및 수정 | 100% 완벽한 코드 보장 |
| 이미지 분석 | 이미지 생성 |

못하는 것들 중 일부는 MCP를 연결하면 할 수 있게 됩니다. 이건 챕터 08에서 배웁니다.

---

## 실습: 첫 대화 나눠보기

1. 프로젝트 폴더를 만듭니다:
   ```
   cd C:\claude-projects
   mkdir first-test
   cd first-test
   claude
   ```

2. 아래를 입력해봅니다:
   ```
   이 폴더에 어떤 파일이 있어?
   ```

3. 간단한 요청을 해봅니다:
   ```
   index.html 파일 만들어줘.
   "안녕하세요, 클로드코드!"라는 글자가 화면 가운데 크게 나오는 페이지.
   배경은 연한 노란색.
   ```

4. 결과를 확인합니다:
   ```
   이 HTML 파일을 브라우저에서 열어줘
   ```

5. 수정 요청을 해봅니다:
   ```
   글자 아래에 현재 시간을 표시하는 기능도 추가해줘
   ```

6. 종료합니다:
   ```
   /exit
   ```
