# CLAUDE.md — 프로젝트의 설명서 만들기.

> CHAPTER 12 / 27 · PART 3 · 고구마마켓 · 예상 시간 30분

## 문제: 클코는 기억력이 없습니다

클로드코드는 대화가 길어지면 앞에서 한 얘기를 잊습니다.

새 대화를 시작하면 아예 처음부터입니다. 마치 매일 아침 기억이 리셋되는 비서 같습니다.

### CLAUDE.md가 없을 때 벌어지는 일

```
[월요일]
나: "이 프로젝트는 고구마마켓이라는 중고거래 서비스야. 한국어로 만들어줘."
클코: "네, 한국어 UI로 만들겠습니다."
→ 잘 만들어줌 ✅
```

이런 일이 반복됩니다:

- 어제 정한 디자인 스타일 → 오늘은 전혀 다른 스타일로 만듦
- "가격은 원화로 표시해" → 달러로 표시함
- "Supabase MCP 연결돼 있어" → 모르고 로컬 파일로 데이터 저장하려 함
- 같은 실수를 매번 반복

## 해결책: CLAUDE.md

CLAUDE.md는 프로젝트 폴더에 넣어두는 설명서 파일입니다.

클코가 실행될 때 자동으로 읽습니다. 매번 같은 말을 반복할 필요가 없습니다.

```
CLAUDE.md가 하는 일:

"나는 고구마마켓이라는 한국어 중고거래 서비스를 만들고 있고,
Next.js + Supabase를 쓰고, 가격은 원화로 표시하고,
Supabase MCP가 연결돼 있어."
```

### CLAUDE.md가 있으면

| 상황 | 없을 때 | 있을 때 |
|------|--------|--------|
| 새 대화 시작 | "이 프로젝트 뭐야?" 매번 설명 | 자동으로 프로젝트 파악 |
| 디자인 스타일 | 매번 달라짐 | 일관된 스타일 유지 |
| 언어 | 영어로 만들 때도 있음 | 항상 한국어 |
| MCP 사용 | MCP 있는지 몰라서 안 씀 | MCP를 적극 활용 |

## CLAUDE.md 만드는 법

### 방법 1: /init 명령어 (추천)

클코 대화창에서 아래를 입력합니다:

```
/init
```

클코가 프로젝트를 자동으로 분석해서 CLAUDE.md를 생성합니다.

파일 구조, 사용 기술, 설정 파일 등을 보고 알아서 작성합니다.

`/init`은 이미 코드가 좀 있는 프로젝트에서 특히 유용합니다.

클코가 코드를 분석해서 기술 스택, 패턴 등을 자동으로 파악합니다.

### 방법 2: 직접 요청하기

프로젝트 초기에 아무 코드도 없을 때는, 직접 만들어달라고 합니다:

```
이 프로젝트에 CLAUDE.md 만들어줘.

포함할 내용:
```

## CLAUDE.md에 뭘 넣어야 하나?

| 항목 | 설명 | 예시 |
|------|------|------|
| 프로젝트 이름 | 이 프로젝트가 뭔지 | 고구마마켓 — 중고거래 서비스 |
| 기술 스택 | 어떤 기술을 쓰는지 | Next.js, Supabase, Tailwind CSS |
| 언어/화폐 | UI 언어, 화폐 단위 | 한국어 UI, 원화(₩) |
| 연결된 MCP | 어떤 MCP가 있는지 | Supabase MCP 연결됨 |
| 디자인 규칙 | 어떤 스타일인지 | 모던, 깔끔, 모바일 반응형 |
| 기타 규칙 | 특별히 지켜야 할 것 | 이미지는 URL 방식으로 처리 |

## 고구마마켓 CLAUDE.md 예시

아래는 우리가 만들 고구마마켓의 CLAUDE.md 예시입니다:

```
# 고구마마켓

중고 물품을 사고팔 수 있는 웹 서비스.

## 기술 스택

- Next.js (App Router)
- Supabase (데이터베이스, 인증)
- Tailwind CSS (스타일링)
- TypeScript

## MCP

- Supabase MCP 연결됨 — DB 조작 시 MCP를 통해 직접 수행

## 규칙

- 한국어 UI
- 가격은 원화(₩) — "₩10,000" 형태로 표시
- 모바일 반응형 필수
- 디자인은 깔끔하고 모던한 스타일
- 색상 테마: 주황색 계열 (고구마 컨셉)

## 주요 기능

- 상품 목록 (메인 페이지)
- 상품 등록/상세/수정/삭제
- 소셜 로그인 (카카오/구글)
- 결제 (토스페이먼츠)

## 데이터베이스
```

처음에는 간단하게 시작하고, 기능이 추가될 때마다 업데이트하면 됩니다.

## CLAUDE.md는 어디에 위치하나?

프로젝트 폴더의 **최상위(루트)**에 위치해야 합니다:

```
goguma-market/
├── CLAUDE.md          ← 여기!
├── package.json
├── src/
│   └── app/
│       └── page.tsx
└── ...
```

다른 폴더 안에 넣으면 클코가 못 찾습니다.

반드시 프로젝트 폴더 바로 아래에 있어야 합니다.

## 언제 업데이트하나?

프로젝트에 큰 변화가 있을 때 업데이트합니다:

### 새 기능을 추가했을 때

```
CLAUDE.md에 소셜 로그인 기능 추가한 거 반영해줘.
카카오랑 구글 로그인을 지원한다고 적어줘.
```

### 새 MCP를 연결했을 때

```
CLAUDE.md에 Context7 MCP 연결된 거 추가해줘
```

### 디자인 규칙이 바뀌었을 때

```
CLAUDE.md에 폰트는 Pretendard를 사용한다고 추가해줘
```

### 실수가 반복될 때

클코가 같은 실수를 반복하면, CLAUDE.md에 명시합니다:

```
CLAUDE.md에 이 규칙 추가해줘:
"이미지 컴포넌트는 next/image 대신 일반 img 태그를 사용할 것"
```

## CLAUDE.md 작성 팁

### 팁 1: 간결하게 쓰세요

```
# 나쁜 예 — 너무 장황함
이 프로젝트는 2026년에 시작된 프로젝트로, 중고 물품을 거래할 수 있는
플랫폼을 만드는 것이 목표입니다. 사용자는 회원가입을 하고...

# 좋은 예 — 핵심만
```

클코는 길게 쓴다고 더 잘 이해하지 않습니다. 핵심만 짧게 쓰세요.

### 팁 2: 규칙은 구체적으로

```
# 나쁜 예 — 모호함
- 디자인 예쁘게

# 좋은 예 — 구체적
```

### 팁 3: 점점 키워가세요

처음에 완벽한 CLAUDE.md를 만들 필요 없습니다.

```
1주차: 프로젝트명, 기술 스택, 기본 규칙
2주차: + DB 테이블 정보, 디자인 규칙
3주차: + 로그인 방식, API 패턴
4주차: + 결제 연동 정보
```

프로젝트와 함께 CLAUDE.md도 성장합니다.

## 확인 체크리스트

- CLAUDE.md 파일이 프로젝트 최상위 폴더에 있는가?
- 프로젝트 이름과 기술 스택이 적혀 있는가?
- 한국어 UI, 원화 표시 등 규칙이 적혀 있는가?
- Supabase MCP 연결 정보가 적혀 있는가?
- 새 대화를 열었을 때 클코가 프로젝트를 바로 이해하는가?

## 핵심 정리

1. CLAUDE.md = 클코에게 주는 프로젝트 설명서
2. 프로젝트 최상위 폴더에 위치해야 함
3. 클코가 대화 시작할 때 자동으로 읽음 → 매번 설명 안 해도 됨
4. `/init` 명령어로 자동 생성 가능
5. 프로젝트가 커질수록 함께 업데이트 — 기능 추가, MCP 연결, 규칙 변경 시 반영
6. 간결하고 구체적으로 작성하는 게 핵심

다음 챕터에서는 드디어 고구마마켓을 만들기 시작합니다!
